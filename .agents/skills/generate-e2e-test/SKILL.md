---
name: generate-e2e-test
description: 指定UCのクリティカルな画面フローに対するPlaywright E2Eテストを作成する。
---

リポジトリルートの `AGENTS.md` を読み、[共通手順](../../../.claude/commands/generate-e2e-test.md) を実行する。
パスとコマンドはリポジトリルートを基準とする。共通手順内の `/コマンド名` はCodexでは対応する `$skill-name`、または同じ手順ファイルの実行として解釈する。
Gateの正本は `.claude/rules/00-global.md`。承認済みという記録を推測で作らない。
