# 05-plan-approval.md — Plan承認時のルール

## 承認前の要約出力（必須）

Planモード中にユーザーへプランの承認を求める（`ExitPlanMode` を使う）際は、
承認を求める直前に、プランの内容を要約したテキストを必ず出力すること。

- 要約は箇条書きで、実装対象・変更範囲・完了条件が分かる粒度にする
- プラン全文をそのまま貼り付けるのではなく、要点を絞った要約にする
- 要約を出力した上で承認を求める（要約なしでいきなり承認を求めない）
