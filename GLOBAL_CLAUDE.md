# Global Claude Code Memory

> このファイルは `~/.claude/CLAUDE.md`（ユーザーグローバル設定）のテンプレートです。
> 各自の環境にコピーして利用してください。プロジェクト固有情報は含めず、汎用ルールのみを記載します。

## 言語・スタイル設定
- 返答は日本語で行うこと
- コードコメントは英語、説明文は日本語
- コミットメッセージは英語

## Git 操作ルール
- SSH 接続を使用すること（`git@github.com:...`）

## ドキュメント管理ルール（必須）

Planモードで設計・決定した内容は、以下の3ファイルに必ず記録・更新すること。
実装開始前にこれらのファイルを読み込み、内容を把握した上で作業に入ること。

| ファイル | 用途 | 記載内容 |
|---------|------|---------|
| `PLAN.md` | 開発計画 | フェーズ計画・実装対象・完了条件・PoC成功条件 |
| `BACKGROUND.md` | システム背景 | 導入背景・課題・方針・位置づけ |

### ルール詳細

1. **Planモード終了時**: 決定した計画・仕様・背景を該当ファイルに必ず反映する
2. **タスク開始時**: 2ファイルをすべて読み込んでからコードに着手する
3. **仕様変更時**: 実装と同時に該当ファイルを更新する（ドキュメントと実装を乖離させない）
4. **ファイルが存在しない場合**: 最初のPlanモード完了時に新規作成する

### 実装前の Docs 更新・レビューステップ（必須）

**実装コードに着手する前に**、以下の順序で必ず実施すること。

**Step 1: Docs 更新**（実装の前に行う）

以下の `docs/ai-context/` ファイルが計画内容と乖離していないか確認し、漏れがあれば更新する。

| 確認対象ファイル | 確認ポイント |
|---|---|
| `docs/ai-context/project-summary.md` | 技術スタック・フォーカス・リポジトリ構成が計画と合っているか |
| `docs/ai-context/module-map.md` | 新規追加予定のディレクトリ・ファイルの役割が記載されているか |
| `docs/ai-context/glossary.md` | 新しい用語・略語・ステータス定義が追加されているか |
| `docs/ai-context/common-commands.md` | 新コンポーネントの実行コマンドが記載されているか |
| `docs/architecture/overview.md` | アーキテクチャ図・コンポーネント一覧が計画と合っているか |
| `PLAN.md` | フェーズ・チェックリスト・完了条件が最新の計画を反映しているか |
| `BACKGROUND.md` | 背景・技術選定理由が実装判断と一致しているか |

**Step 2: ユーザーレビュー**（Docs 更新後、実装前に必ず実施）

Docs 更新が完了したら、実装に着手する前にユーザーに確認を求める。
「Docs の更新が完了しました。内容を確認の上、問題なければ実装を開始します。」

**Step 3: 実装**（レビュー承認後のみ）

### コミット・Push ルール

- **コミット・Push はユーザーから明示的にコマンドで指示された場合のみ実行する**
- AI が自律的にコミット・Push することは禁止（「一旦Pushして」等の明示的な指示が必要）
- コミットメッセージは英語で記述する

### ファイル命名規則

グローバルスコープでプランニングファイルを作成する場合（プロジェクトディレクトリ外など）は、以下の命名規則に従うこと。

```
{プロジェクト名}_{タスク名}.md
```

**例:**
- `mcp_trial_v2_auth_design.md`
- `my_app_db_migration_plan.md`

**ルール:**
- プロジェクト名とタスク名はスネークケース（小文字 + アンダースコア）で記述する
- タスク名は作業内容を端的に表す英単語を使う
- プロジェクトルート内に `PLAN.md` / `BACKGROUND.md` が置ける場合は従来通りのファイル名を使う（命名規則はグローバルスコープ専用）

## よく使うパターン
- 監査ログは JSONL 形式で `logs/audit.jsonl` に記録
