# accuracy-improvement-backlog.md — 指標ごとの精度向上余地整理

> **位置づけ**: Gate 0〜4のパイプラインには属さないリファレンスドキュメント。開発完了後のトライアル運用で「シグナル/指標がどれくらい実用的か」を評価した際に、**どこに手を入れると精度・実用性が上がるか**を指標単位ですぐ参照できるようにする目的で作成。
> 現時点ではあくまで調査・整理のみで、ここに書かれた改善案はまだ着手・合意していない（実施する場合は個別にADR/CRを起票する）。
> 更新方針: トライアルで得られた気づき（「この指標は誤検知が多かった」等）は都度この下に追記する（`known-pitfalls.md`と同様、リファクタしない・記録を消さない）。
>
> 前半（サマリー表〜データ基盤）は「既存指標のデータ基盤・計算方式に起因する精度の天井」を扱う。後半の[機能・ロジック面で除外・持ち越しになった提案](#機能ロジック面で除外持ち越しになった提案指標データ基盤以外)は、`PLAN.md`・`docs/history/plan-archive.md`・ADR群・`requirements.md`/`use-cases.md`に散在する「精度・実用性は上がりそうだが、開発工数・難易度・既存の設計方針との兼ね合いで見送り／持ち越しにした」機能・ロジック面の提案を横断的に集約したもの。

## 使い方

1. トライアル運用で「シグナルが早すぎる/遅すぎる」「指標がしっくりこない」と感じた銘柄・場面が出てきたら、該当する指標をこの表で引く
2. 「現状」列で今何に基づいて算出しているかを確認し、「精度を高める余地」列にある選択肢のうち費用対効果が良さそうなものから着手を検討する
3. 優先度は「トライアルなしで座学的に見た暫定評価」であり、実際にトライアルで見えた課題の頻度・深刻度で並べ替えてよい

---

## サマリー表

| 指標 | カテゴリ | 現状のデータ基盤 | 主なボトルネック | 改善余地（暫定優先度） |
|---|---|---|---|---|
| RSI | テクニカル | 週次終値14期間、単純平均法 | Wilderの平滑化ではなく単純平均。週足のみ（日足なし） | 中: Wilder方式への切替／低: 日足RSI併用 |
| MACD/シグナル線 | テクニカル | 週次終値12/26/9期間EMA | 週足のみ、標準パラメータ固定 | 低: パラメータの妥当性はほぼ業界標準のため優先度低 |
| MA20/MA75 | テクニカル | 週次終値の単純移動平均 | 短期・長期2本のみ（中期線なし） | 低: 主要トレンド判定は現状で十分 |
| ボリンジャーバンド | テクニカル | 週次20期間、±2σ | σ幅固定（1.5σ/2.5σ等の比較なし） | 低 |
| 出来高・出来高MA20 | テクニカル | J-Quants/Yahoo由来の週次出来高 | 週足集計のため日中の急変を捉えられない | 低: 週次判断が前提のツールなので許容範囲 |
| 52週高値・安値 | テクニカル | 週次終値52週分 | 終値ベース（日中高値・安値ではない） | 低 |
| 相対力（対市場） | テクニカル | 銘柄13週騰落率−日経平均/S&P500 13週騰落率 | ベンチマーク選定が単純（日経平均 or S&P500の二択） | 中: TOPIXとの比較併用、業種ETFとの比較 |
| 相対力（対セクター） | テクニカル | 保有銘柄内の同一セクター平均騰落率で簡易代用 | J-Quants無料プランに業種別指数がなく簡易代用。**保有銘柄が少ないセクターはサンプル数が偏る** | **高**: J-Quantsプランアップ or 業種別指数の代替取得で本来の相対力に置換 |
| PER/PBR | ファンダ | J-Quants `/fins/summary`直近値 | 単一時点のスナップショットのみ（推移が見えない） | 高: `financial_statements`実装で複数期の推移を保持 |
| ROE | ファンダ | J-Quants直近本決算値 | 四半期決算では空欄（本決算のみ開示） | 中: 四半期ROEの補完算出 |
| 売上高成長率・営業利益成長率 | ファンダ | 前年同期比（直近1期分のみ） | 単年の前年比のみ。趨勢（連続増収か一過性か）が見えない | **高**: 複数期の時系列化で「増収increasing trend」を判定可能に |
| EPS成長率・PEGレシオ | ファンダ | 直近1期のEPS成長率から算出 | 同上（単年） | **高**: 同上。複数期あれば平均成長率でPEGの安定性向上 |
| 自己資本比率 | ファンダ | J-Quants直近値 | 単一時点 | 中: 推移が見えると財務健全性の「改善/悪化トレンド」が分かる |
| 営業利益率（`operating_margin`） | ファンダ | JP=直近本決算の`営業利益÷売上高`実測、US=Finnhub `operatingMarginTTM`（CHG-0012／ADR-0011で財務健全性フィルタの4条件目に追加） | 単一時点。JP=決算期ベース／US=TTMで算出方法が混在（`peg_ratio`と同じ割り切り）。閾値10%はバックテスト未経過の叩き台 | 中: 複数期の推移で「利益率が構造的に低下しているか一時的か」を判定。閾値は他のシグナル閾値と同様トライアル運用実績でキャリブレーション |
| 配当利回り・配当性向 | ファンダ | J-Quants直近値（本決算のみ） | 単一時点、四半期は空欄 | 中: 複数期の配当推移で「累進配当か減配リスクか」を判定 |
| 財務データの遡及期間 | データ基盤 | J-Quants無料プラン＝実質2年弱・12週遅延 | **全ファンダ指標の精度の根本的な天井**。1年強のデータでは業績トレンド判定は困難 | **最高**: J-Quants Light（5年/1,650円）またはStandard（10年/3,300円）へのプラン変更 |
| シグナル閾値（RSI70、PEG2.0、52週−10%等） | シグナル判定 | ADR-0004の初期パラメータ（叩き台）を採用 | 本人のポートフォリオ・投資スタイルに対するバックテストを経ていない固定値 | **高**: トライアル運用の実績を基にした閾値キャリブレーション |

---

## 詳細

### テクニカル指標系（`app/Services/Analysis/TechnicalIndicatorCalculator.php`）

#### RSI

- **現状**: 週次終値14期間、単純平均法（Wilderの平滑化ではない）で算出（[TechnicalIndicatorCalculator.php:86-116](app/Services/Analysis/TechnicalIndicatorCalculator.php#L86-L116)）
- **精度を高める余地**:
  - Wilder方式（指数平滑）への切替。単純平均法は直近の値動きに引きずられやすく、Wilder方式より過敏に反応する傾向がある
  - 週足に加えて日足RSIを併用し、週足で拾えない短期の過熱/反落を補足する（ただし「人間が目で見て判断できるシンプルさ」というADR-0003の設計方針とはトレードオフになる）
- **優先度**: 中（実害が出た場合のみ着手が妥当。RSI単体の誤差より、後述のシグナル閾値キャリブレーションの方が効果が大きい可能性が高い）

#### MACD・MACDシグナル線

- **現状**: 週次終値の12/26週EMAの差分＝MACD、その9週EMA＝シグナル線（[TechnicalIndicatorCalculator.php:121-175](app/Services/Analysis/TechnicalIndicatorCalculator.php#L121-L175)）。パラメータは業界標準の12/26/9固定
- **精度を高める余地**: パラメータ自体はほぼ業界標準のため変更の必要性は低い。強いて言えば週足以外（日足MACD）の併用だが、投資スタイル（中長期3ヶ月〜1年）には週足で十分
- **優先度**: 低

#### 移動平均線（MA20/MA75）・ボリンジャーバンド

- **現状**: 週次単純移動平均。ボリンジャーバンドは20週・±2σ固定（[TechnicalIndicatorCalculator.php:53-78](app/Services/Analysis/TechnicalIndicatorCalculator.php#L53-L78)）
- **精度を高める余地**: σ幅のバックテスト（1.5σ/2σ/2.5σの比較）、中期線（MA40等）の追加
- **優先度**: 低（現状の2本線構成は中長期投資判断として妥当な範囲）

#### 出来高・出来高MA20、52週高値・安値

- **現状**: 週次OHLCVの出来高列をそのまま利用。追加のAPI連携は不要（ADR-0004）
- **精度を高める余地**: 週次集計のため日中の出来高急増を取りこぼす可能性はあるが、週次判断ツールという前提上、優先度は低い
- **優先度**: 低

#### 相対力（対市場）

- **現状**: 銘柄13週騰落率 − ベンチマーク（日経平均 or S&P500）13週騰落率（[TechnicalIndicatorCalculator.php:183-194](app/Services/Analysis/TechnicalIndicatorCalculator.php#L183-L194)）
- **精度を高める余地**: TOPIXとの比較併用（日経平均は値がさ株の影響を受けやすい）、業種ETFとの比較
- **優先度**: 中

#### 相対力（対セクター）★改善余地が大きい

- **現状**: J-Quants無料プランに業種別指数（TOPIX-17等）がないため、**保有銘柄内の同一セクター平均騰落率で簡易代用**（ADR-0004、[known-pitfalls.md:44-49](docs/ai-context/known-pitfalls.md#L44)）
- **ボトルネック**: 保有銘柄が少ないセクターではサンプル数が1〜2銘柄しかなく、統計的にほぼ意味を持たない可能性がある。これは指標自体の計算式の問題ではなく、**データソースの制約**が原因
- **精度を高める余地**: J-Quantsをスタンダード/プレミアムプランに上げれば業種別指数（TOPIX-17）を取得でき、本来の「セクター全体の値動き」との比較に置き換えられる。前回整理した財務データの遡及期間（Light/Standard）とあわせて検討すると、Standardプラン（3,300円/月）なら**財務データ10年分＋業種別指数の両方**が一度に解決する
- **優先度**: 高

---

### ファンダメンタルズ指標系（`app/Services/MarketData/JQuantsClient.php` / `fundamental_indicators`）

#### PER・PBR・ROE・自己資本比率・配当利回り・配当性向

- **現状**: いずれもJ-Quants `/fins/summary`の**直近1時点のみ**を`fundamental_indicators`にUPSERT保存（現在値キャッシュ、履歴は持たない設計。[data-model.md:186-204](docs/architecture/data-model.md#L186-L204)）
- **ボトルネック**: 「今のPERが割高か割安か」を判断する material が**その銘柄自身の過去のPERレンジ**と比較できない。同様にROE・配当性向も「今期だけたまたま良い/悪い」のか「継続的な傾向」なのか区別できない
- **精度を高める余地**: 前回整理した`financial_statements`テーブル（Gate3で設計済み・未実装）を実装し、複数期のPER/ROE/配当推移を保持する。これにより「自社ヒストリカルレンジ内でのPER位置」のような、単純な絶対値比較より説得力のある判断材料が作れる
- **優先度**: 高

#### 売上高成長率・営業利益成長率・EPS成長率（PEGレシオ）

- **現状**: いずれも「前年同期比」の**単年の値のみ**（`revenue_growth`/`operating_income_growth`/`eps_growth`、[data-model.md:197-198](docs/architecture/data-model.md#L197-L198), [202](docs/architecture/data-model.md#L202)）
- **ボトルネック**: 単年の成長率は一過性の要因（特需・前期の反動増など）に引きずられやすい。「継続的な増収増益トレンドか」を見るには複数期の時系列が必要
- **精度を高める余地**: `financial_statements`実装により3〜5期の成長率推移を保持し、①平均成長率（PEGレシオの安定性向上）、②成長率の加速/減速トレンド判定、が可能になる
- **優先度**: 高（前述のPER/ROE等の改善と同じ`financial_statements`実装がボトルネック解消の鍵になるため、まとめて着手するのが効率的）

---

### データ基盤（全ファンダ指標に共通する天井）

- **現状**: J-Quants無料プランのため、財務データは実質「12週間前〜2年12週間前まで」（≒1年強）しか取得できない（[known-pitfalls.md:44-49](docs/ai-context/known-pitfalls.md#L44)、`requirements.md`の前提）
- **ボトルネック**: 上記「複数期の推移」系の改善案は、いずれも**J-Quantsのデータ遡及期間そのものが1年強しかない**限り効果が限定的（3〜5期分を確保できない可能性がある）
- **精度を高める余地**: J-Quants Light（5年/1,650円/月）またはStandard（10年/3,300円/月・業種別指数も同時開放）へのプラン変更。ファンダ系・相対力（対セクター）の両方の改善余地を解消する**唯一かつ最も費用対効果の高い施策**
- **優先度**: 最高（他のファンダ系改善案の前提条件になっているため、着手するならこれが最初）

---

### シグナル判定ロジック（`app/Services/Analysis/SignalDeterminationService.php`）

- **現状**: RSI70以上からの反落、MACDデッドクロス、BB上限到達、52週高値−10%、PEG2.0以上、対市場相対力マイナス転換、出来高1.5倍急増＋株価下落 の7種類。閾値はいずれもADR-0004策定時の**初期パラメータ（叩き台）**で、本人のポートフォリオに対するバックテストは未実施（[SignalDeterminationService.php](app/Services/Analysis/SignalDeterminationService.php)）
- **ボトルネック**: 閾値が理論値・一般的な目安のままで、実際のシグナル発生頻度・的中率（その後株価が実際に下がったか）が検証されていない
- **精度を高める余地**: トライアル運用でシグナル発生履歴と実際の値動きを突き合わせ、閾値（RSI70→75、PEG2.0→2.5等）を調整する。あるいは複数シグナルの同時発生を「強いシグナル」として重み付けするロジックの追加
- **優先度**: 高（データ取得元を変えるより低コストで、かつトライアル運用そのものが検証データになるため、真っ先に着手しやすい）

### 内部計算ロジックの精度課題（2026-09-06、CHG-0013／ADR-0012 の調査で発見。低優先で持ち越し）

> ADR-0012（成長率のFY比較化）・バグB（押し目買いPEG下限）は同CRで是正済み。C〜Gは同じ監査で見つかった残りの課題、H〜Kは`/review`（6観点並列エージェント、2026-09-06）で追加判明した課題。いずれも実害は限定的なため次に該当箇所へ手を入れる際に対処する。

| # | 箇所 | 内容 | 影響 |
|---|---|---|---|
| C | `FundamentalIndicatorMapper::annualGrowth()` / `UsFundamentalIndicatorMapper::calculateOperatingIncomeGrowth()` | 前期の値がマイナス（赤字）だと `(当期−前期)/前期` の符号が反転。黒字転換（−100→+50）が「−150%減益」に見える。ガードは `==0` のみ | 「減益」判定・成長率チップの誤り。赤字→黒字の回復銘柄で発生。**2026-09-19追記**: CHG-0017（ADR-0015 D3）の実装中、成長率取得の精度について本人から質問あり。現状整理: JP株は本決算(FY)どうしの前期比、US株はFinnhubのTTM（直近12ヶ月ローリング）を採用しており算出方法が異なる（`peg_ratio`と同じ既知の割り切り）。精度向上の効果が最も大きいのはJ-Quantsプランアップグレード（下記「データ基盤」節、全体優先度2位。現状の遡及期間の薄さが`averageAnnualGrowth()`〔Cycle2〕の複数期平均の精度上限になっている）。本バグC自体も低コストで直せる候補として残す |
| D | `FinnhubClient::fetchReportedFinancials()` | `JQuantsClient::fetchStatements()` と違い並べ替えなしで `data[0]`=当期前提。Finnhubが古い順で返すと `equity_ratio` が誤年・`operating_income_growth` が符号反転 | US株の自己資本比率・営業利益成長率。現状のFinnhub応答順に依存 |
| E | `FinnhubClient::findConceptValue()` | XBRL は同一 concept を複数期コンテキストで返す。先頭一致だと前期比較値を拾うリスク（ADR-0009 で検証済みとあるが再確認推奨） | US株の実測算出値 |
| F | `FetchExternalMarketDataAction`（セクター平均相対力） | `sectorReturnBuckets` に自銘柄の騰落率を含めて平均している（薄いセクターで信号が鈍る）。「簡易算出」と明記済み | 対セクター相対力（JP株）。既知の割り切り |
| G | `TechnicalIndicatorCalculator`（week52_high/low） | 週足終値の最大/最小であり、ザラ場高値/安値ではない | 52週高値からの下落率等がやや浅めに出る。一般的な簡略化 |
| H | `FetchExternalMarketDataAction`（`financial_statements`保存） | `revenue_yoy_change`/`operating_income_yoy_change`は「最新の開示行（`fetchStatements()`の index 0）」にのみ付与する既存パターン（ADR-0012以前から不変）。直近開示が本決算でなく四半期決算の場合、その四半期行に前期通期比の値が付き、実際の本決算行はnullのままになる。現状Blade側は`historical_performance`のyoy_change列を描画していないため実害なし（`/review`確認済み） | UC-006の過去業績推移に将来この列を表示する場合、行と数値の対応がずれる。表示追加時にindex 0ではなくperiod_type==='FY'の行へ付け替える改修が必要 |
| I | `FundamentalIndicatorMapper::map()` / `FetchExternalMarketDataAction`（`annualGrowth()`呼び出し） | `map()`内でnet_sales/operating_profit/epsの3フィールド分`annualGrowth()`を個別呼び出し（FY2行の絞り込み・重複排除・ソートを3回重複実行）。`FetchExternalMarketDataAction`側も`revenue_growth`/`operating_income_growth`と同じ値をrevenue_yoy_change/operating_income_yoy_change用に再計算（`$fundamental`に既にある値を使わず二重計算）。1銘柄あたり本来1回で済む処理を計5回実行 | 実害はパフォーマンスのみ（保有銘柄数×5倍の配列処理）。`resolveTwoMostRecentFyStatements()`等への切り出しで解消可能 |
| J | `SignalCriteriaEvaluator::classify()`の`lte_positive`分岐 | 既存`lte`分岐（met/near/unmet判定ロジック）をほぼそのままコピーし、値≦0のガードのみ追加（ADR-0012 D4のPEG下限用）。`lte`側のバッファ仕様が将来変わると2箇所が乖離するリスク | PEGレシオの判定チップのみ対象。実害は無いが保守性の課題 |
| K | `JQuantsClient::toFloatOrNull()` / `toStringOrNull()`（ADR-0012で追加） | null/空文字列ガードが同一の2メソッドが並立（キャスト型のみ異なる） | 実害なし。共通ヘルパーへの統合は任意 |

---

## 全体の優先順位まとめ（暫定）

1. **シグナル閾値のキャリブレーション**（追加コストほぼゼロ。トライアルの実績データがあれば着手可能）
2. **J-Quantsプランのアップグレード**（Light/Standard、月額1,650〜3,300円。ファンダ系・相対力〔対セクター〕双方の天井を一括解消）
3. **`financial_statements`テーブルの実装**（2の効果を実際に活かすための保存・時系列化の受け皿。Gate3設計済み・未着手）
4. その他（RSI計算方式の見直し、ベンチマーク多様化等）は上記3点の効果を見てから着手を判断

---

## 機能・ロジック面で除外・持ち越しになった提案（指標データ基盤以外）

> 上記はいずれも「既存指標の計算式・データ基盤」を対象にした改善余地。ここでは対象を広げ、**機能・判定ロジックそのものの提案**のうち、開発工数・難易度・既存の設計方針との兼ね合いで見送り／持ち越しになったものを集約する。個別の経緯は出典（`PLAN.md`・`docs/history/plan-archive.md`・ADR・`requirements.md`/`use-cases.md`）を参照。

| 提案 | 除外・持ち越しの理由 | 現在地 | 出典 |
|---|---|---|---|
| チャート形状（波形）パターン検出（ゴールデンクロス/デッドクロス・三尊天井等） | 判定ロジックの優先順位づけ・パターン検出の実装難易度が未整理で、Phase1のスコープからは持ち越し | `requirements.md`のINスコープには記載済み（F-004候補）だが未実装。着手時期未定 | `docs/product/requirements.md:31`、`docs/history/plan-archive.md:106,416,434` |
| 出来高を判断ロジック（シグナル判定）にどう組み込むか（優先度含め） | `requirements.md`が「判断材料の優先順位①出来高」と明記する一方、具体的な組み込み方針が未決定のまま持ち越し | ADR-0004で出来高の指標自体（出来高・出来高MA20）は追加済みだが、シグナル判定ロジックへの優先度づけは未整理 | `docs/history/plan-archive.md:415,421,433` |
| 利確・リバランス閾値の動的分岐ロジック（銘柄の状態に応じて閾値を可変にする） | 分岐軸（シグナル発生有無・ファンダ成長性等）の設計が固まっておらず、UC-004/UC-009双方への反映方法も未確定のため「検討事項・未着手」として記録のみ | `use-cases.md`のUC-009に「検討中」の注記のみ追加。本文の閾値記述は未変更。着手判断はユーザー指示待ち | `PLAN.md:53-78`、`docs/product/use-cases.md:507` |
| ML的な合成特徴量・DCF法等の高度な分析手法 | 「人間が目で見て判断できるシンプルさ」というADR-0003の設計方針を損なうため、ADR-0004策定時に意図的に見送り。標準的な指標（出来高・52週高値安値・PEGレシオ・相対力）の追加に留めた | 見送り継続中（設計哲学と対立するため、再検討には別途ADRでの方針転換が必要） | `docs/adr/ADR-0004-analysis-engine-indicator-expansion.md:54` |
| システム内蔵のLLMチャット・対話機能（その場で質問しながら判断を絞り込む体験） | 「当面は自由記述メモ欄＋外部LLMサービスの併用」で代替可能と判断し、開発工数の大きいシステム内蔵チャットは将来検討に位置づけ | `requirements.md` OUTスコープに明記。実装時期未定 | `docs/product/requirements.md:38,48` |
| ~~整理対象（損切り）候補一覧: 含み損×長期保有×ファンダ不健全×回復兆候なしの銘柄を機械的に足切り提案する、UC-004（利確検討）の鏡像機能~~ | **→ 2026-09-05 着手（F-011・UC-011・ADR-0010・CHG-0010）**。`requirements.md`2章OUTを部分改訂しF-011としてINへ（F-010がADR-0007で押し目買いに限りOUTを覆したのと同じ構図）。スコープは「一覧＋判断材料の提示のみ」（分割売却の指値提案・自動発注はしない）、UC-009統合なし、DBスキーマ変更なし（表示レイヤー完結）。保有期間は「観測以来の連続保有週数のみ」で割り切り、`holdings`への取得日カラム追加・手動上書きは今回入れない（将来の余地として残す）。新規閾値3件（整理検討ライン-20%／52週高値-30%／押し目0件）はGate4の`/tdd`サイクルで実データを見て確定 | Gate1/2/3承認済み。Gate4（`/tdd`）実施中 | 本ドキュメント（会話記録）、`docs/adr/ADR-0010-loss-review-candidate-list.md`、`docs/product/use-cases.md` UC-011 |
| 週次/月次の売買代金・出来高ランキング（今どの銘柄が買われている/売られているかのトレンド情報）の機械的取得・表示 | `requirements.md`2章OUTの「メタトレンド（社会的要因等で強くなるセクター）自体の自動発見・機械的スコアリングは対象外、注目テーマは本人が人力で登録する方針」に抵触する領域のため、着手には当該OUT記述の再改訂（CR）＋ADR起票が別途必要。楽天証券の外国株ランキング画面を発端に本人から相談があり、技術調査のみ実施済み | 未着手（要件未改訂）。技術調査の結論（2026-09-05）: 証券会社ランキング画面のスクレイピングは規約面（楽天は自動ツールでの情報取得を利用規約で明示的に禁止）・技術面（JS動的レンダリングで静的取得不可）の両方でリスクが高く非推奨。代替として、日本株は契約中のJ-Quants APIで全銘柄`daily_quotes`を取得し売買代金/出来高ランキングを自前算出、米国株は無料枠のあるAlpha Vantage `TOP_GAINERS_LOSERS`エンドポイントを使う組み合わせが無料・信頼度・保守性のバランス最良と判断（Finnhubは市場全体のランキング型エンドポイント自体が存在せず今回の用途には不向きと判断済み）。次のプラン検討時に候補として扱う | 本ドキュメント（会話記録） |
| `hold`（キープ）バケツ内の「好調で優先的に強調したいキープ銘柄」のハイライト（F-013） | ポートフォリオ分類ダッシュボード（UC-013、ADR-0014）のGate2レビューで本人から提案。相対力・RSI健全域等を組み合わせた「好調キープ」の判定基準が現時点でどのUCにも存在せず、新規に閾値を発明することになるため第1段階では見送り（`hold_watch`＝悪化方向の早期警戒は第1段階から実装済み。好調方向のハイライトのみ持ち越し） | 未着手（判定基準未設計）。第1段階の運用実績（`hold`バケツの実データ分布）を見てから、具体的な基準（例: 相対力プラスかつRSI健全域50〜70）を検討する | `docs/adr/ADR-0014-portfolio-bucket-classification.md` D5、`docs/product/use-cases.md` UC-013承認記録（2026-09-17） |
| 20%アクティブ枠の予算トラッキング（積立コア70〜80%・残り20%でアクティブ売買という運用像を、目標額・投下済み額・余力の3値で数値管理する、F-013 D7） | 余力（預り金・買付余力）が楽天証券の保有CSVに含まれず別入力が必要で、`requirements.md`2章OUT「預り金等の取込」に抵触するため別CRが必要。F-013第1段階は「保つ」の内訳表示（積立コア vs キープ）に留めた | 未着手（別入力手段の設計待ち）。投下済み額（`core_accumulation`の`market_value`）は既にF-013第1段階で見える状態 | `docs/adr/ADR-0014-portfolio-bucket-classification.md` D7 |

### 備考

- 上記のうち「チャート形状パターン検出」「出来高の判断ロジック組み込み」「利確・リバランス閾値の動的分岐」は、いずれも**要件自体は否定されていない**（将来のPhaseで着手する前提の持ち越し）。「ML的な合成特徴量・DCF法」「システム内蔵LLMチャット」は**設計方針・スコープ判断として意図的に対象外**にした点で性質が異なる
- いずれも着手する場合はGate 0〜4のパイプライン（`.claude/rules/00-global.md`）に従い、Planフェーズからやり直す（動的分岐ロジックは`PLAN.md`記録時点で「着手時はPlanフェーズから開始」と明記済み）

## 売買戦略の深化ロードマップ（2026-09-19、一次資料2件＋別セッションのベンチマークより）

> **位置づけ**: 本節は「全項目の実装を約束するロードマップ」ではない。3つの資料の提案を現行実装と突き合わせたうえで、**次に着手する1手のみを詳細設計**し、残りは前提コスト・効果見込みを添えた**候補リスト**として記録する。次の1手の実測結果（本ドキュメントの他の項目と同様、導入前後の影響銘柄数を実データで比較する方式）を見てから、候補リストのどれに進むかを個別に判断する。全項目を一括で実装する設計にはしない（機能追加の複雑化を避けるための意図的な絞り込み）。

### 資料の位置づけ

- **資料1**: `docs/original-docs/stock_auto_order_strategy_notes.md`（スコアリング層の拡張案①〜⑦・出口戦略・インフラ）
- **資料2**: `docs/original-docs/売買戦略2.txt`（現行コードの実装レビュー。バリュー/景気敏感銘柄が構造的に取りこぼされる問題の指摘）
- **資料3**: 別セッション「戦略の調査とレコメンド」の内容として2026-09-19に本人から共有（会話上のテキストのみ・ファイル化はされていない）。著名手法（O'Neil／Minervini SEPA／Peter Lynch GARP／Weinstein／Bogleheads／Piotroski F-Score）と現行戦略を比較し、**現行戦略はこれらのハイブリッドであり「車輪の再発明ではない」**と評価したうえで、各手法との差分から7つの改善候補を提示したもの

資料2（実装レビュー）と資料3の一部項目が、**独立した切り口から同一のコード箇所**（`FundamentalHealthEvaluator`の成長率救済・PEG基準、`BuySignalDeterminationService`の相対力フォールバック）を指摘している。この一致が、複数候補の中から「次の1手」を1つに絞り込む決め手になっている。

### 突き合わせ結果（現行実装との対照）

**資料1 — スコアリング層の拡張案**

| 提案 | 現状 | 判定 |
|---|---|---|
| ①テクニカル層（RSI/MACD/MA20乖離/52週/BB/PEG） | ADR-0004で実装済み | 実装済み |
| ②ファンダ層 | 指標取得・営業利益率判定（CHG-0012）実装済み。同業種内相対評価は未実装 | 部分的 |
| ③マクロ・セクター文脈層 | F-007は日経平均・S&P500のみ。米10年債・VIX・USD/JPYは未実装で常にnull。対セクター相対力は算出済みだが判定未使用 | 部分的 |
| ④意思決定支援層（グループ分類） | F-013／ADR-0014がまさにこれ（Gate1/2承認済み・実装待ち） | 進行中 |
| ⑤ダイナミック・ファクター・ローテーション | 記載なし | 未検討 |
| ⑥IPOイベントリスク（ロックアップ解除日） | 記載なし | 未検討 |
| ⑦信用取引需給層 | 記載なし | 未検討 |
| ATRトレーリングストップ | 固定2段階（+20%/+150%）のみ。週次OHLCVは既存で新規取得なしに算出可能 | 未実装 |
| 通知基盤（LINE Messaging API） | `app/Notifications/`自体が存在しない | 未実装 |
| バックテスト基盤＋モンテカルロ | コード無し。`requirements.md`で明示的にOUT | 未実装・要スコープ改訂 |

**資料2 — 実装レビュー（バリュー/景気敏感銘柄の取りこぼし）**

| 提案 | 現状 | 判定 |
|---|---|---|
| 成長率バグ／PEG下限バグ | ADR-0012／CHG-0013で是正済み | 対応済み |
| 成長率救済分岐（3〜5期平均） | `FundamentalHealthEvaluator`は単年度OR判定のみ。`financial_statements`に16期分が既に保存済みで新規取得なしに算出可能 | 未実装・即着手可 |
| PEG依存の低減（低成長業種で無効化） | 除外分岐なし | 未実装 |
| 買い増し条件(B)の対セクター化 | `relative_strength_vs_sector`は保存済みだが未使用 | 未実装・低コスト |
| スタイルタグ（growth/value_cyclical） | コード・DBに存在しない | 未実装。**2026-09-19追記**: セクター分類ベースでの実装を検討したが`sector_classification_id`がJP保有146銘柄中7銘柄しか埋まっておらず実効性がないと判明し撤回。ADR-0015は財務指標のみで判定する方式に変更（下記「次の1手」参照） |

**資料3 — 著名手法とのベンチマーク**

| 提案 | 現状 | 判定・関連度 |
|---|---|---|
| ①対称的な損切り（-7〜8%機械的） | F-011は-20%で「判断材料提示のみ」。requirements.mdで機械的ロスカットは意図的にOUT | 出口戦略の論点として記録（詳細化はしない） |
| ②押し目買いに中期トレンド健在の確認が薄い | 週足MA20/MA75はあるが週足MA30（Weinstein 30週線相当）は未算出。トレンドの向きを見ていない | 資料2の指摘と同一領域 |
| ③相対力(RS)の活用が弱い | vs_marketのみ使用、vs_sector未使用 | 資料2の指摘と完全一致 |
| ④セクター配分の5/25ルール | requirements.mdの「複雑な最適化はしない」方針と対立 | 保留（設計方針との対立、採るならADR必須） |
| ⑤PEG合否基準・成長率上限レンジの明文化 | 財務健全性4条件にPEGは不在。成長率上限も無く極端値の既知問題あり | 資料2の指摘と同一領域 |
| ⑥ウォッチリストの棚卸しルール未定義 | F-012で216銘柄取込済みだが除外・再確認のタイミング未定義 | 軽量・独立した候補 |
| ⑦Piotroski的トレンド改善視点 | 絶対水準判定のみ。`financial_statements`16期で前期比較は算出可能 | ファンダ履歴蓄積の候補に含める |

### 次の1手（詳細設計） — 判定ロジックの是正

> **2026-09-19追記**: Gate1ドキュメント（ADR-0015・requirements.md・use-cases.md・traceability-matrix.md）を起票済み。CHG-0017。詳細設計・依存関係・採否根拠は `docs/adr/ADR-0015-value-cyclical-stock-judgment-branching.md` を正本とし、以下は着手前に記録した設計メモとして残す。

**選定理由**: 3資料中2つ（資料2・資料3の②③⑤）が独立に同じ問題を指摘しており確度が高い。新規データ取得ゼロ（`financial_statements`16期・`relative_strength_vs_sector`はいずれも既存データ）。既に実害あり（商社/資源株が早期に+20%利確ライン固定、かつ押し目買い候補からも弾かれる）。

対象範囲（1つのCR、複数Cycleに分割してTDD）:
- (a) `FundamentalHealthEvaluator`に「直近3〜5期平均成長率>0%」のOR救済経路を追加（`financial_statements`16期を利用、全銘柄共通）
- (a2) **2026-09-19改訂・再改訂**: `FundamentalHealthEvaluator`に「ROE≧15%かつ自己資本比率≧50%」を満たせば成長率を問わず救済する第3の経路を追加（全銘柄共通、セクター非依存）。当初は「配当利回り≧3%」もAND条件に含めていたが、無配当・低配当でも資本効率を高める米国株等を除外してしまうとの指摘で撤回（実測: 保有218銘柄中、配当必須なら救済0件・配当不問なら2件救済）。配当利回りは参考表示のみ
- (b) PEG判定（売り側`peg_overvalued`・買い側`peg_undervalued`の両方）を**成長率が低い（5%以下、暫定閾値）銘柄**で無効化し、PER/配当利回りの絶対閾値へ代替（2026-09-19改訂: 発動条件をセクターベースの「value_cyclical」から成長率データそのものに変更）。**(a)/(a2)と(b)は同一設計単位**——成長率救済のみではPEGシグナルが`signalCount`を非ゼロに保ち続け、`TakeProfitThresholdEvaluator`の高水準モードに到達できないことがコヒーレンスレビューで判明（ADR-0015 Context参照）。PEGの合否基準（growth銘柄側）・成長率上限レンジの明文化（資料3⑤）は別候補として切り出し、本CRのスコープには含めない
- (c) 買い増し事前条件(B)を`relative_strength_vs_sector`が非nullなら対セクター、nullなら対市場へフォールバック
- (d) 押し目買いの前提に「中期トレンド健在」条件を追加。**Gate1で決定: 週足MA75の傾き判定を採用**（直近値が13週前の値を上回るか。新規指標MA30より低コスト。`technical_indicators`へ軽量な新規カラムを追加する小規模migrationが必要）

> **(e) スタイルタグ（`sector_classification_id`からの機械マッピング）は2026-09-19、Gate4 Cycle1着手時に撤回**。実データで`sector_classification_id`がJP保有146銘柄中7銘柄しか埋まっておらず（CR発端の伊藤忠8001も未分類）実効性がないと判明。加えて本人から「セクター分類自体に重きを置いていない」「食品はgrowthではない、建設も文脈次第」との指摘があり、業種名による二分自体を撤回し、(a2)(b)の財務指標ベースの判定に統合した。詳細はADR-0015参照

**効果測定方法**（本ドキュメント既存項目・CHG-0012と同じ実測比較の方式）: 導入前後で「財務健全性passed/failedの銘柄数」「押し目買い候補に新たに該当する銘柄数」を実データで比較し、想定通りの銘柄（商社・資源株等）が救済されるかを本人が確認してから採否を決める。この実測結果が、下記候補リストのどれに進むかを左右する一次情報になる。

資料2末尾の5つの確認事項は、Gate1で決着済み（ADR-0015参照）: 判定軸は含み益率ベースを継続、UC-004とUC-010の同時表示はせずUC-004側に補助バッジ（`valuation_zone_badge`）を追加、PER≦15かつ配当利回り≧3%を暫定閾値として実装時に実測、スタイル分類の粒度は上記の通りセクターベースを撤回し財務指標ベースに変更。実際に取得できている期数は実測済み（146銘柄中84銘柄に`financial_statements`保存済み、平均8期）。

### 候補リスト（軸のみ・詳細設計はしない・実装は約束しない）

| # | 候補 | 前提コスト | 効果の見込み | 出典 | 関連する既存項目 |
|---|---|---|---|---|---|
| A | ファンダ指標の週次履歴スナップショット蓄積 | 低（取込時に1行INSERT追加のみ） | 高（PER/PBRの自社レンジ評価・トレンド改善視点(資料3⑦)の土台。データは時間でしか貯まらないため着手が遅れるほど不利） | 資料2フェーズ2 | 本節「PER・PBR・ROE・自己資本比率・配当利回り・配当性向」「財務データの遡及期間」 |
| B | ATRベースの動的トレーリングストップ＋損切りルールの再検討 | 低（週次OHLCVは既存） | 中〜高（銘柄ごとのボラ差を無視した固定閾値の弱点を解消。資料3①の対称的損切りの論点も含む）。**2026-09-19追記（本人要望）**: 利確を含み益率の閾値到達だけでなく「山まで上げて一回下げてきた」というチャート形状（`week52_high_pullback`等の技術シグナル）の確認とセットで判定したい、という要望あり。現状`TakeProfitThresholdEvaluator`は技術シグナルを参考表示に留め対象抽出には使っていないため、この要望はATRの話（損切り幅の話）とは別に「利確トリガーへの技術シグナル組み込み」として本候補の設計時に明示的に扱う | 資料1、資料3①、本人要望（2026-09-19） | 「機能・ロジック面で除外・持ち越しになった提案」の「利確・リバランス閾値の動的分岐ロジック」 |
| C | 信用取引需給層 | 中（新規データソース調査が必要） | 中（JP株限定。週次公表サイクルとの相性は良い） | 資料1⑦ | なし（新規） |
| D | ウォッチリスト棚卸しルールの明文化 | 極小（運用ルールのみ、コード変更不要の可能性） | 低〜中 | 資料3⑥ | なし（新規） |
| E | シグナル的中率の検証基盤（バックテスト＋モンテカルロ） | 高（要件スコープ改訂・ADR必須。`requirements.md`で明示的にOUT） | 将来的には最大（閾値キャリブレーションの前提）だが遠い | 資料1 | 「シグナル判定ロジック」節「シグナル閾値（RSI70、PEG2.0、52週−10%等）」＝全体優先順位1位の項目 |
| F | マクロ・セクター文脈層の穴埋め（VIX/米10年債/SOX）＋市場全体タイミングを買いトリガーに組み込み | 低（`MarketIndexClient`にティッカー追加） | **2026-09-19、優先度を格上げ（本人要望）**: 「財務健全性が高くかつ割安と判断される銘柄を、市場全体が良いタイミングで買っていきたい」という本人の買い方の核に直結する層。「次の1手」（value_cyclical銘柄の健全性・割安判定の是正）が完了すると「健全×割安」の銘柄プールが正しく機能するため、本候補はその後に来る自然な第2弾。単体着手（指標穴埋めのみ）でも低コストで先行可能だが、真の価値は「健全×割安」プールと組み合わせて買いトリガー化すること（⑤ファクターローテーションを待たずに着手する価値がある） | 資料1③、本人要望（2026-09-19） | なし（新規） |
| G | IPO/ロックアップ解除日 | 中（データソース調査） | 低（保有銘柄の該当数を実データ確認してから判断） | 資料1⑥ | なし（新規） |
| H | セクター配分5/25ルール | 中（設計方針との対立、ADR必須） | 不明（方針転換の是非を先に問う必要） | 資料3④ | なし（新規） |
| I | ダイナミック・ファクター・ローテーション | 高（AとEが前提） | 不明（最も高度、時期尚早） | 資料1⑤ | なし（新規） |
| J | 通知基盤（LINE Messaging API） | 中 | 低〜中（Eの「シグナル発動履歴DB記録」と同じ土台のためセット検討が効率的） | 資料1 | なし（新規） |
| K | PEGの優先度低下・PER/PBR中心の判定への切り替え | 中（既存7シグナルのうちPEG系2つの再設計＋呼び出し元の判定チェックリスト改修） | **2026-09-19追記（本人指摘、CHG-0017／ADR-0015 D3実装中に発生）**: PEG＝PER÷成長率は「価格（PER）」と「将来予測（成長率）」という2つの不確実な数値の比率であり、PER/PBR単体より構造的に脆い（分母の成長率がノイズに弱く、低成長時に機械的に肥大化する問題がD3の出発点）。実データ検証で判明した具体例: 東京エレクトロン（8035、単年度成長率0.5%・PER41）はD3のPEG除外ロジックで`peg_overvalued`シグナルを失うが、PER41は絶対水準として明確に割高であり、本来は「一時的な踊り場」であって除外すべきではない銘柄。対処法は2通りある——(a)`isLowGrowth()`を単年度だけでなく複数期平均成長率（Cycle2の`averageAnnualGrowth()`）も見る形に拡張して精度を上げる、(b)そもそもPEGの重み・優先度を下げてPER/PBR単体の判定に寄せる。本人の判断で(b)方向を支持し、CHG-0017では(a)の追加工数はかけずCycle3a/3bの単年度ベース実装で止めることを選択。この論点は資料2の確認事項5「PEGレシオ依存を減らす」（`accuracy-improvement-backlog.md`資料2セクション参照）とも直結する | 本人指摘（2026-09-19）、資料2 | 資料2フェーズ1「PEG依存の低減」、候補A（PER/PBR履歴レンジ評価の前提） |

| L | 判定チェックリスト（`SignalCriteriaEvaluator`）をD1〜D3のレスキュー・除外分岐に追従させる | 中（`fundamentalRows()`の成長率行・`evaluateBuy()`/`evaluateTakeProfit()`のPEG行を、D1〔ROE/自己資本比率救済〕・D2〔3期平均成長率OR救済〕・D3〔低成長銘柄のPEG除外→PER/配当利回り代替〕の各分岐を認識する形に再設計する必要がある。D4〔対セクター相対力フォールバック〕は`SignalCriteriaEvaluator::evaluateLossReview()`の「相対力」行のみ2026-09-21に対応済み〔コミット1f785d2〕） | **2026-09-21追記（CHG-0017 `/review`3回目の指摘、Cycle6）**: `SignalCriteriaEvaluator`の判定チェックリストはD1/D2/D3のいずれの分岐にも一切追従しておらず、D1/D2でレスキューされた銘柄はfundamental_status='passed'（バッジ緑）なのに成長率チェックリスト行は「unmet」（矛盾）、D3で低成長PER/配当利回り代替により`peg_undervalued`が発生した銘柄もPEGチェックリスト行は「unmet」のまま（実際の合格根拠がPER/配当利回りであることが一切表示されない）。実害は表示の矛盾のみ（実際の判定ロジック自体は正しい）だが、D4分（`相対力`行のみ）の対応で示した設計（行ラベルを動的に切り替える等）を、成長率行・PEG行にも拡張する必要があり、D1〜D3の3分岐×2行分の設計判断（新設の行を追加するか、既存行のラベル・基準値を動的に切り替えるか）が要るため次の1手の実測結果を待たずスコープ外として記録 | /review（2026-09-21、Cycle6） | 資料2フェーズ1「PEG依存の低減」、候補K |

候補A〜Lは、次の1手の実測結果が出るまで着手判断を保留する。着手する場合はGate 0〜4のパイプラインに従い、Planフェーズからやり直す。

## 関連ドキュメント

- `docs/adr/ADR-0004-analysis-engine-indicator-expansion.md`（指標セットの選定理由・初期閾値の位置づけ）
- `docs/ai-context/known-pitfalls.md`（J-Quantsプラン制約の詳細）
- `docs/architecture/data-model.md`（`technical_indicators`/`fundamental_indicators`/`financial_statements`のテーブル定義）
- `PLAN.md` / `docs/history/plan-archive.md`（機能・ロジック面の検討経緯の一次情報。本ドキュメントの「機能・ロジック面で除外・持ち越しになった提案」節の出典）
- `docs/product/requirements.md`（スコープIN/OUTの正本。将来検討・対象外の判断根拠）
- `docs/original-docs/stock_auto_order_strategy_notes.md`／`docs/original-docs/売買戦略2.txt`（上記「売買戦略の深化ロードマップ」節の資料1・資料2。参照のみ・編集禁止）
