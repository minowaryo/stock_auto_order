# use-cases.md — ユースケース定義

> **重要度: ★★★★★（最重要）**
> 対象: 人間レビュー最終ポイント
> ここで仕様品質が決定する。AIのコード生成品質はこの資料の質に依存する。

---

## レビュー基準

各ユースケースに以下が定義されているか確認する:

- [ ] Actor が明確か
- [ ] 入力が定義されているか
- [ ] 出力が定義されているか
- [ ] エラーが定義されているか
- [ ] 業務ルールが明記されているか
- [ ] 状態変更が明確か
- [ ] 権限考慮があるか
- [ ] CRUD が網羅されているか

---

## ユースケース一覧

| UC | 機能ID | 名称 |
|---|---|---|
| UC-001 | F-001 | CSV取込 |
| UC-002 | F-002 | 保有銘柄一覧表示 |
| UC-003 | F-003 | 銘柄詳細表示 |
| UC-004 | F-004 | 利確シグナル一覧 |
| UC-005 | F-005 | セクター配分ダッシュボード |
| UC-006 | F-006 | 新規投資候補の重複チェック |
| UC-007 | F-007 | 市場全体指標表示 |
| UC-008 | F-008 | 新規投資候補レコメンド（軽量版） |
| UC-009 | F-009 | 取込後サマリーレポート |
| UC-010 | F-010 | 既存保有株の買い増しタイミングレコメンド |
| UC-011 | F-011 | 整理検討（含み損）候補一覧 |
| UC-012 | F-012 | お気に入り未保有銘柄ウォッチリスト（UC-006/UC-008を統合・刷新、ADR-0013） |
| UC-013 | F-013 | ポートフォリオ分類ダッシュボード（減らす／保つ／増やす、ADR-0014） |

---

### UC-001: CSV取込

**Actor**: 本人
**前提条件**: 楽天証券から国内株式CSV・米国株式CSVをダウンロード済み（両方必須）。投資信託CSVは任意でダウンロード済み
**関連要件**: F-001

#### 基本フロー
1. 本人がCSV取込画面で国内株式CSV・米国株式CSVを選択し（必須）、任意で投資信託CSVも選択してアップロードする
2. システムがCSVの種別（JP株/US株/投資信託）を判定する
3. システムがCSVをパースし、保有銘柄・数量・取得価格を抽出する
4. システムが取込結果を新しい週次スナップショットとして保存する（既存スナップショットは上書きせず、履歴として蓄積する）
5. システムが今回のスナップショットを直前の週次スナップショットと比較し、直前スナップショットに存在しなかった銘柄（銘柄コード＋市場区分の組み合わせ）を「新規検出銘柄」として記録する
6. システムが取込結果（成功件数・エラー件数・新規検出銘柄数）を表示する
7. システムが今回の取込バッチに含まれる銘柄（JP株・US株の個別株のみ、ADR-0004）について、外部データを同期的に取得し、テクニカル指標（RSI・MACD・ボリンジャーバンド・移動平均線・出来高・52週高値/安値・相対力）・ファンダメンタルズ指標（PER・PBR・ROE・売上高/営業利益成長率・自己資本比率・営業利益率・配当利回り/性向・EPS成長率・PEGレシオ）を計算し保存する。データソースはJP株がJ-Quants・Yahoo Finance相当、US株はYahoo Finance相当（株価）・Finnhub（ファンダメンタルズ指標、ADR-0009）を用いる。あわせて市場全体指標（日経平均・S&P500等）の週次スナップショットを取得・保存する（相対力〔対市場〕の算出に使用。F-007ダッシュボード画面自体はPhase2）
8. システムが上記指標をもとに利確シグナル（UC-004）を判定し保存する
9. システムが上記指標をもとに買い増しシグナル（UC-010）を判定し保存する。利確シグナル（フロー8）とは判定対象・保存先ともに独立しており、含み益率による対象銘柄の絞り込みは行わない（ADR-0007）
10. システムが取込完了と同時に取込後サマリーレポート（UC-009）を自動生成し表示する

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| jp_stock_file | ファイル（CSV） | 必須 | 拡張子`.csv`、Shift-JISエンコード、最大サイズ5MB |
| us_stock_file | ファイル（CSV） | 必須 | 拡張子`.csv`、Shift-JISエンコード、最大サイズ5MB |
| mutual_fund_file | ファイル（CSV） | 任意 | 拡張子`.csv`、Shift-JISエンコード、最大サイズ5MB |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| import_batch_id | int | 取込バッチのID |
| status | string | `pending` / `processing` / `completed` / `failed`（`glossary.md`参照） |
| imported_count | int | 正常に取り込んだ銘柄数 |
| error_count | int | スキップした行数 |
| imported_at | datetime | 取込日時 |
| newly_detected_symbols | array<string> | 新規検出銘柄（銘柄コード＋市場区分）のリスト。保有銘柄一覧（UC-002）のNEWバッジ表示、および新規投資候補の重複チェック（UC-006）への自動遷移に用いる |

#### 業務ルール
- 国内株式CSV・米国株式CSVは両方揃って初めて取込を実行できる。どちらか一方のみアップロードされた場合は取込を実行せずエラーとする（投資信託CSVはこの必須条件に含まれない）
- 数値項目（数量・単価等）はカンマ区切り・クォート付き文字列として解釈し数値に変換する
- CSV内の個別行がパースできない場合、その行はスキップしてエラー件数に計上し、他の行の取込は継続する
- ファイル全体が読み込めない（文字コード不正・フォーマット全体が想定外）場合は取込全体を失敗とする
- 同一銘柄が複数のCSV種別（JP株/US株/投信）にまたがる場合、種別ごとに別レコードとして扱う
- 新規検出銘柄の判定は直前1回分の週次スナップショットとの比較のみで行う（それ以前の履歴は参照しない）。銘柄コード＋市場区分が完全一致する場合は「既存」とみなす
- 初回取込（比較対象となる直前スナップショットが存在しない）の場合は、判定基準がないため全銘柄を「新規検出銘柄」として扱わない
- 新規検出銘柄フラグは当該スナップショットが「直近」である間のみ有効とする（次回取込で新しいスナップショットが最新になった時点で、前回の新規検出銘柄フラグは表示上の意味を持たなくなる）
- 楽天証券のJP株/US株CSVはファイル内が口座区分（特定口座/一般口座/NISA成長投資枠等）ごとに複数セクションへ分かれている。投資信託CSVはセクション分割ではなく行ごとの`口座区分`列（値: `特定`/`NISAつみたて投資枠`等）で口座区分を表す。同一銘柄が複数の口座区分にまたがる場合、従来通り銘柄コード単位で保有数量を合算し、取得単価は加重平均で算出したうえでスナップショット（`holding_snapshots`）に保存する。加えて、口座区分ごとの内訳（数量・取得単価）を`holding_snapshot_accounts`に追記保存する（ADR-0002）。この内訳は利確シグナル一覧（UC-004）・セクター配分ダッシュボード（UC-005）でNISA区分（NISA成長投資枠/NISAつみたて投資枠）を除外する判定に使用する
- US株はCSVヘッダーに記載された参考為替レート（例: 159.32円/USD）を用いて取込時に円換算し、円建ての評価額・評価損益をスナップショットに保存する
- 銘柄の識別は「銘柄コード（JP株の証券コード／US株のティッカー／投資信託のファンド名）」+「市場区分（JP/US/投信）」の組み合わせで一意に扱う
- 外部データ取得（フロー7〜9、ADR-0004/ADR-0007）は個別銘柄単位で行い、特定銘柄の取得に失敗しても取込全体は失敗させない。失敗した銘柄は該当指標が`null`（「取得不可」）のまま保存し、取込結果（フロー6）自体は継続する（UC-003エラーケース「指標計算に必要な外部データが取得できない」と同じ扱い）

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| ファイル未選択 | 422 | CSVファイルを選択してください |
| 国内株式CSV・米国株式CSVのいずれか一方のみアップロード | 422 | 国内株式・米国株式のCSVは両方アップロードしてください |
| 拡張子が`.csv`以外 | 422 | CSVファイルのみアップロードできます |
| ファイルサイズ超過 | 413 | ファイルサイズが上限（5MB）を超えています |
| ファイル全体がパース不能 | 422 | CSVの形式を確認してください（文字コード・列構成が想定と異なる可能性があります） |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-002: 保有銘柄一覧表示

**Actor**: 本人
**前提条件**: 直近のCSV取込（UC-001）が1回以上完了している
**関連要件**: F-002

#### 基本フロー
1. 本人が保有銘柄一覧画面にアクセスする
2. システムが直近の週次スナップショットの保有銘柄を一覧表示する
3. システムが各銘柄の含み益率・利確シグナル有無を一覧内に表示する
4. システムが直近のCSV取込（UC-001）で新規検出された銘柄に「NEW」バッジを表示する
5. 本人が任意の銘柄（NEWバッジ部分を除く）を選択すると銘柄詳細（UC-003）に遷移する
6. 本人がNEWバッジを選択すると、対象銘柄コードが入力された状態で新規投資候補の重複チェック（UC-006）に遷移する

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| sector（フィルタ） | string | 任意 | 既存セクター分類のいずれか |
| signal_only（フィルタ） | boolean | 任意 | シグナル発生銘柄のみ表示 |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| symbol_code | string | 銘柄コード |
| symbol_name | string | 銘柄名 |
| market | string | 市場区分（JP/US/投信） |
| instrument_type | string | 銘柄種別（個別株/ETF/投資信託） |
| quantity | decimal | 保有数量（複数口座区分にまたがる場合は合算値） |
| average_cost | decimal | 取得単価（複数口座区分にまたがる場合は加重平均） |
| current_price | decimal | 現在値（投資信託は基準価額） |
| unrealized_gain_rate | decimal | 含み益率（%） |
| sector | string | セクター分類（未分類含む） |
| has_signal | boolean | 利確シグナル有無 |
| rsi | decimal | RSI（14期間）。テクニカル指標の代表値として一覧に簡易表示する |
| per | decimal | PER。ファンダメンタルズ指標の代表値として一覧に簡易表示する |
| revenue_growth | decimal | 売上高成長率（%、前年同期比）。会社業績の代表値として一覧に簡易表示する |
| is_newly_detected | boolean | 直近のCSV取込（UC-001）で新規検出された銘柄かどうか（NEWバッジ表示に使用） |

#### 業務ルール
- 直近の週次スナップショットのみを表示対象とする（過去スナップショットの閲覧は本フェーズの対象外）
- 利確シグナルが立っている銘柄は一覧上で視覚的に強調表示する
- テクニカル/ファンダメンタルズ指標の計算・利確シグナル判定（UC-003/UC-004）はJP株・US株の個別株のみを対象とする。ETF・投資信託は一覧には表示するが、`has_signal`は常にfalse、指標欄（`rsi`/`per`/`revenue_growth`含む）は「対象外」として扱う
- 一覧では情報過多を避けるため、テクニカル・ファンダメンタルズ・会社業績それぞれの代表指標1つずつ（RSI・PER・売上高成長率）のみを小さなバッジ形式で表示する。指標の全項目（MACD・ボリンジャーバンド・ROE等）は表示せず、銘柄詳細（UC-003）への遷移で確認する構成とする
- `is_newly_detected=true`の銘柄は一覧上で「NEW」バッジを表示する。バッジのクリックのみ新規投資候補の重複チェック（UC-006）へ遷移し、行のその他の部分のクリックは従来通り銘柄詳細（UC-003）へ遷移する

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| 取込データが1件も存在しない | - | 空状態として「CSV取込が必要です」を表示し、UC-001への導線を示す |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-003: 銘柄詳細表示

**Actor**: 本人
**前提条件**: 保有銘柄一覧（UC-002）に対象銘柄が存在する
**関連要件**: F-003

#### 基本フロー
1. 本人が一覧から対象銘柄を選択する
2. システムが直近の株価推移チャート（デフォルト3年、MA20・MA75・取得単価の基準線を重ねて表示）を表示する。本人は表示期間（1年/3年/5年/10年）を切り替えられる
3. システムがテクニカル指標（RSI・MACD・ボリンジャーバンド・移動平均線）を計算し表示する
4. システムがファンダメンタルズ指標（PER・PBR・ROE・売上高/営業利益成長率・自己資本比率・営業利益率・配当利回り/性向）を表示する
5. システムが利確シグナルの判定結果と判定根拠を表示する
6. 本人がメモ欄に自由記述（LLMとの壁打ち内容の転記等）を入力し保存する

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| chart_period | string | 任意 | `1y`/`3y`/`5y`/`10y`のいずれか。省略時は`3y`（デフォルト） |
| memo | text | 任意 | 最大2000文字 |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| symbol_code / symbol_name | string | 銘柄基本情報 |
| average_cost | decimal | 取得単価。株価推移チャート上に基準線として表示する |
| price_history | list | `chart_period`で選択した期間の株価推移データ。各要素は`date`/`close_price`/`ma20`/`ma75`を持つ（週次スナップショット由来） |
| rsi / macd / bollinger_band / ma20 / ma75 | decimal等 | テクニカル指標一式（直近値） |
| volume / volume_ma20 | decimal | 直近出来高・20週平均出来高（ADR-0004） |
| week52_high / week52_low | decimal | 52週高値・52週安値（ADR-0004） |
| relative_strength_vs_market / relative_strength_vs_sector | decimal | 相対力（直近13週の騰落率と、対市場〔日経平均/S&P500〕・対セクター〔保有銘柄内の同一セクター平均〕の騰落率との差、%）。対セクターはJ-Quants無料プランに業種別指数がないため保有銘柄内平均で簡易算出する（ADR-0004） |
| per / pbr / roe / revenue_growth / equity_ratio / operating_margin / dividend_yield | decimal等 | ファンダメンタルズ指標一式 |
| eps_growth / peg_ratio | decimal | EPS成長率、PEGレシオ（PER÷EPS成長率）。EPS成長率が0以下の場合`peg_ratio`は`null`（算出不可）とする（ADR-0004） |
| signal_result | string | シグナル判定結果（例: 利確検討） |
| signal_reason | string | 判定根拠（例: RSIが72から65に反落） |
| memo_history | list | 保存済みメモ一覧（日付付き） |

#### 業務ルール
- メモは自由記述とし、保存時にシステムが日付を自動付与する（過去のメモは編集せず追記していく）
- ファンダメンタルズ指標はJP株がJ-Quants由来のため最大12週間遅延する可能性がある旨を画面上に明示する。US株はFinnhub由来（ADR-0009）であり、この12週遅延の制約は適用されない（決算開示のタイミングに応じて更新される）
- 株価推移チャートは週次スナップショットに蓄積済みの終値データをそのまま用いて描画する（新規のリアルタイム取得・重い計算は行わない）
- 期間切り替え（`chart_period`）は表示範囲の絞り込みのみであり、RSI等の指標計算期間には影響しない（指標自体は`docs/architecture/data-model.md`（Gate 3）で定義する固定期間で計算する）
- チャートはあくまで人間が保有継続/利益確定を判断するための参考情報であり、チャート単体からの自動売買判定・シグナル生成は行わない（自動判定はUC-004の対象）
- `peg_ratio`はEPS成長率が0以下（減益・横ばい）の場合、指標として意味を持たないため算出せず`null`（「算出不可」）とする（ADR-0004）
- `relative_strength_vs_sector`は同一セクターの保有銘柄が本人以外1件も存在しない場合、比較対象がないため`null`（「算出不可」）とする。画面上は「保有銘柄内の同一セクター平均に対する相対力」である旨を明示する（J-Quants無料プランでは業種別指数が取得できないための簡易算出、ADR-0004）

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| 指標計算に必要な外部データが取得できない | - | 該当指標のみ「取得不可」と表示し、他の指標・メモ機能は通常通り利用可能とする |
| 選択期間に対応する株価データが存在しない（新規上場銘柄等） | - | 取得可能な範囲のみでチャートを表示し、「〇年分のデータのみ表示しています」等の注記を出す |
| メモが2000文字超 | 422 | メモは2000文字以内で入力してください |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-004: 利確シグナル一覧

**Actor**: 本人
**前提条件**: 保有銘柄のテクニカル指標が計算済み（UC-003の計算処理に依存）
**関連要件**: F-004

#### 基本フロー
1. 本人が利確シグナル一覧画面にアクセスする
2. システムが含み益+20%超の銘柄を抽出する
3. システムが各銘柄のシグナル種別（RSI高水準からの反落・MACDデッドクロス等）を判定する
4. システムが分割指値の提案（例: +20%地点で1/3、+35%地点で1/3、残りはトレンド追従）を表示する
5. システムが各銘柄について、なぜ利確を検討すべきか（またはまだ検討不要か）を平易な一言サマリとして一覧に表示する
6. 本人が銘柄をクリックすると、銘柄詳細（UC-003）に遷移し、判定の背景・全指標を確認できる

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| なし | - | - | - |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| symbol_code / symbol_name | string | 銘柄基本情報 |
| market_value | decimal | 評価額（保有数量 × 現在値）。US株はCSVヘッダーの参考為替レートで円換算済みの `current_price` を用いるため円建て（2026-09-05追加、CHG-0011）。一覧では銘柄名の右隣に表示する |
| unrealized_gain_rate | decimal | 含み益率（%） |
| signal_types | list | 発生しているシグナル種別のリスト |
| signal_reason_summary | string | 判定根拠の一言サマリ（例: "含み益+32%到達、RSIが高値72から65へ反落中。過熱感が和らぎつつあり利確を検討するタイミングです"）。シグナル未発生の銘柄には、なぜまだシグナルが出ていないかを示す文言を表示する |
| split_limit_suggestion | list | 分割指値の提案（価格帯・売却割合）。NISA区分（NISA成長投資枠/NISAつみたて投資枠）を除いた課税口座（特定口座/一般口座）保有数量を上限として算出する |
| criteria | object | 判定チェックリスト（下記業務ルール参照）。`technical`（7項目）・`fundamental`（3項目）の各配列と、`summary`（グループごとの `met`/`near`/`total` 件数）を持つ。各項目は `label`・`threshold_label`（基準の表示文字列、例: `≥70`）・`value_label`（実測値の表示文字列、例: `72.1`）・`status`（`met`／`near`／`unmet`／`unavailable`）を持つ |
| valuation_zone_badge | string \| null | 成長率が低い銘柄で、現在の絶対バリュエーション（PER・配当利回りの絶対閾値）でもなお割安と判定される場合に「絶対バリュエーション上は割安ゾーン」を表示する補助バッジ（2026-09-19追加・改訂、ADR-0015 D7。セクター分類には依存しない）。該当しない場合はnull。UC-010（買い増し候補）への同時掲載は行わない |

#### 業務ルール
- 含み益が「利確検討ラインの閾値」を超えない銘柄は対象外（`requirements.md`のスコープに従う）。閾値は下記の動的分岐ルールにより銘柄ごとに決まる
- 対象はJP株・US株の個別株のみとし、ETF・投資信託は対象外とする（`UC-002`業務ルール参照）
- シグナル種別（`signal_type`）は以下7種とする。値は`docs/architecture/data-model.md`の初期パラメータ値と同様、叩き台として実装時に確定する（ADR-0004）
  - `rsi_reversal`（RSI高水準からの反落）
  - `macd_dead_cross`（MACDデッドクロス）
  - `bollinger_overheat`（ボリンジャーバンド上限付近での過熱）
  - `week52_high_pullback`（52週高値からの反落。初期値: 52週高値から-10%以上下落）
  - `peg_overvalued`（PEGレシオによる割高化。初期値: PEGレシオ2.0以上）
  - `relative_strength_weakening`（相対力の低下。初期値: 対市場の相対力が直近4週でプラスからマイナスに転換）
  - `volume_spike_decline`（出来高急増を伴う下落。初期値: 当該週の出来高が20週平均の1.5倍以上、かつ株価が前週比下落）
- 分割指値の提案は固定ルールではなく例示であり、具体的な閾値・比率のパラメータ化（銘柄ごとの調整可否を含む）は`docs/architecture/data-model.md`（Gate 3）で確定する
- **利確検討ラインの動的分岐（2026-08-28確定、CHG-0006）**: 中長期戦略の趣旨に反し、下落兆候が一切なく財務的にも健全な銘柄まで一律+20%で利確検討対象にしてしまう問題を解消するため、銘柄ごとに以下の2モードのいずれかを適用する
  - **通常モード**（下記の高水準モードの条件を満たさない銘柄）: 含み益+20%超で対象、分割指値は+20%地点で1/3・+35%地点で1/3・残りトレンド追従（従来通り）
  - **高水準モード**（「現在シグナルが0件」かつ「`docs/architecture/data-model.md`の財務健全性フィルタ〔UC-005/008/009/010と共通の基準〕を満たす」銘柄のみ）: 含み益+150%超で対象、分割指値は+100%地点で1/3・+150%地点で1/3・残りトレンド追従
  - 高水準モードに該当する銘柄の`signal_reason_summary`には、通常より高い水準まで利確検討を保留している旨（例:「シグナルなし・財務健全のため利確ラインを+150%まで引き上げています」）を含める
  - 本ロジックは`signals`テーブルへの永続化タイミング・判定対象（含み益+20%超という既存のシグナル判定条件）には影響しない。UC-010（買い増しタイミングレコメンド）の対象抽出は`signals`の有無を参照するため、本変更によるUC-010への影響はない
- 一覧では技術的なシグナル種別バッジ（RSI高値反落等）に加えて、`signal_reason_summary`を1行で表示し、判断軸が一目でわかるようにする。全指標の内訳・判定の詳細背景は一覧には出さず、銘柄詳細（UC-003）に集約する
- シグナルが発生していない銘柄（含み益+20%超のみ）も一覧の対象に含め、他の銘柄と同様に銘柄詳細（UC-003）への遷移導線を設ける
- **NISA区分（NISA成長投資枠/NISAつみたて投資枠）を除外する（ADR-0002）**: 保有がすべてNISA区分の銘柄は、非課税メリットを維持するため利確シグナル一覧の対象外とする（売却提案できる課税口座保有分が存在しないため）。特定口座/一般口座とNISA区分の両方にまたがる銘柄は、`unrealized_gain_rate`・シグナル判定は引き続き保有数量全体（合算値）を基準に行うが、`split_limit_suggestion`はNISA区分を除いた課税口座保有数量を上限とする
- 含み益が薄い/マイナスの既存保有銘柄への買い増し判断はUC-010（既存保有株の買い増しタイミングレコメンド）で提供する。判定対象・保存先（`buy_signals`）ともに本UCの利確シグナル（`signals`）とは独立しており、同一銘柄に利確シグナルと買い増しシグナルが同時に成立することはない（UC-010業務ルール参照、ADR-0007）
- **判定チェックリスト（2026-08-29追加、CHG-0007）**: `signal_reason_summary` の文章だけでは「基準にどれだけ近いか」「7項目のうち何項目を満たしているか」が読み取れないため、一覧の各銘柄行に、判定に使う項目の「基準点」と「その銘柄の実測値」「達成状態」を並べた表（`criteria`）を表示する。表示のみの機能であり、`signals` の判定・永続化ロジックには一切影響しない
  - **テクニカル7項目**（利確を後押しする方向を「達成」とする）: ①含み益率（>利確ライン。通常+20%／高水準モード時+150%）②RSI（≧70）③52週高値からの下落率（≦-10%）④ボリンジャーバンド上限乖離率（≧0%）⑤MACD−シグナル線（<0）⑥PEGレシオ（≧2.0）⑦相対力〔対市場〕（<0）。基準値はいずれも `docs/architecture/data-model.md` で確定済みのシグナル判定閾値をそのまま用い、新しい基準値は設けない。**成長率〔売上高・営業利益の高い方〕が低い（5%以下、暫定閾値）銘柄はPEGレシオ項目（⑥）を判定対象から除外する（2026-09-19改訂、ADR-0015 D3。セクター分類には依存しない）**。低成長銘柄はPEGが構造的に高くなりやすく、財務健全性が良好でも本項目のみで`met`扱いになり`signalCount`が非ゼロに保たれ続け、下記の高水準モードに到達できなくなるため
  - **財務健全性4項目**（`FundamentalHealthEvaluator` の4条件をそのまま数値展開。2026-09-06、CHG-0012／ADR-0011 で営業利益率を追加し3→4項目）: ROE（≧10%）／自己資本比率（≧40%）／成長率〔売上高・営業利益の高い方〕（>0%）／**営業利益率（≧10%）**。財務が健全であることは利確側では「まだ売らなくてよい」という保留材料であり、テクニカル7項目とは意味の向きが逆になるため、別グループとして区切り、達成件数もテクニカルと分けて集計する。高水準モード該当銘柄では、財務グループの見出しに利確ライン引き上げの根拠である旨を紐づける。**成長率項目は次のいずれかを満たせばOR判定で`met`とする（2026-09-19追加・改訂、ADR-0015 D1/D2）**: ①単年度の売上高/営業利益成長率のいずれかプラス、②直近3〜5期平均成長率>0%（`financial_statements`、JP株のみ・3期未満は対象外）、③ROE≧15%かつ自己資本比率≧50%（成長率を問わない財務指標のみの救済経路、全銘柄対象。配当利回りは判定条件には使わず参考表示のみ——無配当・低配当でも資本効率の高い銘柄〔米国株に多い〕を除外しないため）。景気の谷の年に単年度だけマイナスでも財務が健全なら救済する。銘柄のセクター分類には依存しない
  - **達成状態**: 各項目は「達成（`met`）」「あと一歩（`near`）」「未達（`unmet`）」「データなし（`unavailable`）」の4値。`near` の判定バッファは `docs/architecture/data-model.md`（Gate 3）で定める（基準値の±20%手前。基準値が0の項目は比率が定義できないため達成／未達の2値とする）。実測値が未取得（外部データ取得の失敗等、ADR-0009によりUS株のファンダメンタルズ指標も通常時は取得できる）の項目は `unavailable` とし、実測値欄は「—」と表示する
  - **表示上の扱い**: 「判定の全背景・関連する全指標の内訳は一覧に出さず詳細画面に集約する」という `ui-guidelines.md` テーブル方針の例外として、基準点つきの本チェックリストに限り一覧に表示する。個別指標の時系列・計算根拠の詳細は引き続き銘柄詳細（UC-003）に集約する
- **一覧の並び順（2026-09-17追加、F-013/ADR-0014 D10-1）**: ①シグナル数（多い順）②判定チェックリストのテクニカル達成数（多い順）③含み益率（高い順）の優先順位で並べる。従来は並び順の定義がなく取込順のまま表示されていた既存の穴を、F-013（ポートフォリオ分類ダッシュボード）の`take_profit`バケツ設計にあわせて埋めたもの。新しい閾値・重み付けは設けず、いずれも既存の算出値のみを用いる
- **`valuation_zone_badge`（2026-09-19追加、ADR-0015）**: 成長率〔売上高・営業利益の高い方〕が低い（5%以下、暫定閾値）銘柄が、現在の絶対バリュエーション（PER≦15かつ配当利回り≧3%、暫定閾値）でもなお割安と判定される場合、`valuation_zone_badge`に「絶対バリュエーション上は割安ゾーン」を表示する。**判定基準は成長率のみを参照し、銘柄のセクター分類には依存しない**（2026-09-19改訂、実データで`sector_classification_id`がほぼ未整備と判明したためセクターベースの分岐を撤回）。判定の主軸（含み益率）は変更しないため、この銘柄をUC-010（買い増し候補）へ同時掲載することはしない

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| 対象銘柄が存在しない | - | 空状態として「+20%を超える含み益の銘柄はありません」を表示 |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-005: セクター配分ダッシュボード

**Actor**: 本人
**前提条件**: 保有銘柄にセクター分類情報が付与されている（J-Quants等由来）
**関連要件**: F-005

#### 基本フロー
1. 本人がダッシュボード画面にアクセスする
2. システムが保有銘柄をセクター別に集計し配分比率を表示する
3. システムが各セクターの配分状態（健全/やや偏り/偏り警告の3段階）を判定し、配分バー上に目安ライン（40%）とあわせて表示する
4. システムが偏り警告となったセクターについて、目標配分まで下げるための売却額・株数の目安を算出し表示する
5. システムが保有比率が低い/未保有のセクターの中から、注目テーマ合致＋財務健全性フィルタ（F-008と同じロジック）でリバランス候補銘柄を抽出し、推薦理由と購入額の目安を添えて表示する

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| なし | - | - | - |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| sector_name | string | セクター名（未分類を含む） |
| allocation_rate | decimal | 配分比率（%） |
| allocation_status | string | 配分状態（`健全`/`やや偏り`/`偏り警告`の3段階） |
| is_overweight | boolean | 偏り警告フラグ（`allocation_status`が`偏り警告`の場合true） |
| rebalance_candidates | list | 提案する他セクターの候補銘柄。各要素は`symbol_code`/`symbol_name`/`sector_name`/`reason`（推薦理由の一言サマリ）/`suggested_purchase_amount`（購入額の目安）/`nisa_recommended`（NISA枠での購入推奨フラグ、UC-008参照）を持つ |
| suggested_sell_amount | decimal | 目標配分まで下げるための売却額の目安（円）。NISA区分（NISA成長投資枠/NISAつみたて投資枠）を除いた課税口座保有分のみを対象に算出する |
| suggested_sell_quantity | int | 同、現在値ベースの株数換算の目安（NISA区分を除く） |

#### 業務ルール
- 配分状態は2段階の目安ラインで判定する（初期値・叩き台）: 40%未満=健全、40%以上70%未満=やや偏り、70%以上=偏り警告。両閾値のパラメータ化・変更可否は`docs/architecture/data-model.md`（Gate 3）で確定する
- 配分バーには40%の目安ラインを表示し、現状が目安に対してどの位置にあるか視覚的にわかるようにする
- `健全`のセクターにはバッジを表示しない（情報過多を避けるため）。`やや偏り`/`偏り警告`のみバッジで強調する
- セクター分類が取得できていない銘柄は「未分類」として集計に含める
- 売却額の目安は「（現在配分率−目標配分率〔初期値は偏り判定閾値と同一の70%〕）×保有評価額合計」のシンプルな計算式で概算する。複数銘柄への配分方法・端数処理・銘柄選定ロジックの詳細は`docs/architecture/data-model.md`（Gate 3）で確定する
- リバランス候補銘柄の`reason`・`suggested_purchase_amount`は、F-008（UC-008）の抽出ロジック（本人が登録した注目テーマへの合致＋財務健全性フィルタ、購入額目安はポートフォリオ全体の1〜2%程度）を流用する。候補は`健全`または`やや偏り`のセクターの中から選ぶ（`偏り警告`セクターは対象外）
- あくまで「目安」であり、自動発注は行わない（`requirements.md`のOUTスコープに従う）
- **NISA区分（NISA成長投資枠/NISAつみたて投資枠）を除外する（ADR-0002）**: `allocation_rate`（配分比率）自体はNISA区分を含む保有全体で算出する（保有の全体像を示す指標のため除外しない）。一方`suggested_sell_amount`/`suggested_sell_quantity`の算出・対象銘柄選定は特定口座/一般口座の保有分のみを対象とし、NISA区分の保有分は非課税メリットを維持するため売却提案の対象から除外する。偏り警告セクターの保有がすべてNISA区分の場合、`suggested_sell_amount`/`suggested_sell_quantity`は0（「NISA区分のみ保有のため売却提案なし」として表示）とする
- `rebalance_candidates`の`nisa_recommended`はUC-008と同じ基準で判定する（詳細はUC-008業務ルール参照）

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| セクター分類データが未取得の銘柄がある | - | 該当銘柄は「未分類」として表示を継続し、画面の表示自体は止めない |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-006: 新規投資候補の重複チェック

> **2026-09-06、ADR-0013（F-012）で UC-012 に統合・刷新**: 本UCの価値ある部分（同セクター保有比率＝`overlap_rate` の可視化、テクニカル/ファンダ指標一式、過去業績推移、ウォッチステータス／メモの記録・履歴）は UC-012 の一覧・行展開に引き継ぐ。**「銘柄コードを手入力して1銘柄をチェックする」入口は廃止**し、お気に入りCSV由来のウォッチリストが候補の供給源になる。以下の記述は UC-012 実装までの経緯として残す。

**Actor**: 本人
**前提条件**: 以下のいずれかによって対象銘柄が定まっている
  - (a) 本人が新規投資候補の銘柄コードを把握している（手動入力での利用）
  - (b) 直近のCSV取込（UC-001）で新規検出銘柄として記録されている（保有銘柄一覧UC-002のNEWバッジ経由での利用）
  - (c) おすすめ候補（UC-008）の一覧から選択した（同一画面内での連携）
**関連要件**: F-006

#### 基本フロー
1a. 本人が新規投資候補チェック画面に直接アクセスし、銘柄コードを入力する（手動入力）
1b. 本人が保有銘柄一覧（UC-002）でNEWバッジを選択し、対象銘柄コードが入力された状態で本画面に遷移する（自動検出経由。入力値は変更可能）
2. システムが該当銘柄のセクター分類を取得する
3. システムが既存保有との重複度・分散への影響を判定し表示する
4. システムがテクニカル指標・ファンダメンタルズ指標（銘柄詳細UC-003と同じ項目セット）を判定結果にあわせて表示する
5. システムが直近3〜5期分の売上高・営業利益の推移を表示する
6. システムが対象銘柄について過去に保存されたウォッチステータス・メモがあれば、判定結果とあわせて表示する
7. 本人が判定結果を踏まえてウォッチステータス（様子見/買い時/次回購入候補/リバランス対象）とメモ（任意）を入力し保存する

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| symbol_code | string | 必須 | 証券コード形式（4桁数字等、市場に応じた形式）。UC-002のNEWバッジ経由の場合は初期値として自動入力される |
| watch_status | string | 任意 | `様子見`/`買い時`/`次回購入候補`/`リバランス対象`のいずれか |
| watch_memo | text | 任意 | 最大2000文字 |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| symbol_name | string | 銘柄名 |
| sector | string | セクター分類 |
| overlap_rate | decimal | 既存保有の同セクター内比率（重複度） |
| diversification_comment | string | 分散影響に関するコメント |
| rsi / macd / bollinger_band / ma20 / ma75 | decimal等 | テクニカル指標一式（UC-003と同一項目） |
| per / pbr / roe / revenue_growth / equity_ratio / dividend_yield | decimal等 | ファンダメンタルズ指標一式（UC-003と同一項目） |
| historical_performance | list | 直近3〜5期分の決算期・売上高・営業利益・前年比増減のリスト |
| watch_status | string | 直近保存されたウォッチステータス（`様子見`/`買い時`/`次回購入候補`/`リバランス対象`）。未保存の場合は未設定 |
| watch_memo_history | list | 保存済みウォッチステータス・メモの履歴（日付・その時点のステータス・メモ本文付き） |

#### 業務ルール
- どの銘柄が有望かという選定自体はシステムの対象外とし、既存保有との分類上の重複・分散影響の可視化に限定する。ウォッチステータスはこの方針の例外ではなく、本人が自分の判断を記録するための項目であり、システムによる自動判定・推奨は行わない
- 重複度は「対象銘柄と同一セクターの既存保有比率」を基準に算出する
- 新規に買付した銘柄は少額から様子見で保有を始めるケースが多いため、購入前の事前チェック（フロー1a）だけでなく、CSV取込後に「気づいたら新規銘柄が増えていた」という事後チェック（フロー1b）も主要な利用経路として想定する
- 本画面は単一銘柄をじっくり確認する用途のため、保有銘柄一覧（UC-002）のように代表指標のみへ絞り込まず、銘柄詳細（UC-003）と同等のテクニカル/ファンダメンタルズ指標一式を表示する
- 過去の業績推移（`historical_performance`）はJ-Quantsの財務諸表データに基づく。件数（3〜5期）・具体的な項目は`docs/architecture/data-model.md`（Gate 3）で確定する。ファンダメンタルズ指標と同様、最大12週間遅延する可能性がある旨を画面上に明示する
- 対象銘柄の指標算出に必要なデータが取得できない場合（新規上場間もない等）、該当指標のみ「取得不可」と表示し、他の判定結果（重複度・分散影響等）は通常通り表示する
- 画面はUC-008（新規投資候補レコメンド）と統合し、「新規投資候補」という単一のメニュー項目の下部セクションとして提供する（タブ数を増やさないため。詳細は`ui-guidelines.md`参照）
- ウォッチステータス・メモは銘柄コード単位で保存し、同一銘柄を再度本画面でチェックした際（手動入力・NEWバッジ経由・おすすめ候補経由のいずれでも）に過去の記録を参考情報として表示する
- メモは自由記述とし、保存時にシステムが日付とその時点のウォッチステータスを自動付与する（過去の記録は編集せず追記していく。UC-003のメモ機能と同様の運用とする）
- ウォッチステータスのみの更新（メモ本文なし）も許可する

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| 銘柄コードが存在しない/セクター分類が取得できない | 422 | 銘柄コードを確認してください |
| ウォッチメモが2000文字超 | 422 | メモは2000文字以内で入力してください |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-007: 市場全体指標表示

**Actor**: 本人
**前提条件**: 市場全体指標（日経平均・S&P500・米国10年債利回り・VIX指数・USD/JPY為替レート）が直近の週次スナップショットとして取得済み
**関連要件**: F-007

> 市場全体指標の**取得・週次保存ロジック**（日経平均・S&P500分）は、UC-003の相対力指標（対市場）算出に必要なため、本UC（ダッシュボード画面本体）に先行してPhase 1で実装する（ADR-0004、`requirements.md` 7章参照）。本UCの画面自体（前日比・移動平均乖離の可視化、米国10年債利回り/VIX指数/USD-JPYを含む全指標の表示）はPhase 2のまま変更しない。

#### 基本フロー
1. 本人が保有銘柄一覧画面（UC-002）にアクセスする
2. システムが画面上部のウィジェットに直近の市場全体指標を表示する
3. システムが各指標の前日比・移動平均乖離をシンプルな見え方（矢印アイコン・色分け）で表示する

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| なし | - | - | - |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| index_name | string | 指標名（日経平均/S&P500/米国10年債利回り/VIX指数/USD-JPY） |
| value | decimal | 直近値 |
| change_rate | decimal | 前日比（%） |
| ma_deviation | decimal | 移動平均乖離率（%） |

#### 業務ルール
- 個別銘柄の利確シグナル判定（UC-004）・セクター配分（UC-005）等のロジックには自動反映しない。あくまで人間が景気の強弱・リスクオン/オフを把握するための参考情報とする
- 表示場所は保有銘柄一覧（UC-002）画面上部のウィジェットとし、独立した画面（メニュー項目）は設けない
- 市場全体指標も他のスナップショットと同様に週次で記録し、履歴として蓄積する

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| 一部の指標の外部データ取得に失敗 | - | 該当指標のみ「取得不可」と表示し、他の指標・画面全体の表示は継続する |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-008: 新規投資候補レコメンド（軽量版）

> **2026-09-06、ADR-0013（F-012）で UC-012 に置き換え（Superseded by UC-012）**: 「本人が登録した注目テーマに合致し財務健全性の高い銘柄を提示する」という狙いは、候補供給源（`holdings` 由来＝過去に保有した銘柄のみ・米国株は永久にゼロ件）が構造的に不足しており実運用で機能しなかった。UC-012 では楽天証券お気に入りCSV（216銘柄）が供給源となり、CSVのフォルダ名が「注目テーマ」を兼ねる。`watched_themes` の手動登録は廃止する。財務健全性フィルタ・小口購入額の目安（1%）・NISA推奨は UC-012 に引き継ぐ。**なお F-005（セクター配分ダッシュボードのリバランス候補）が流用する `NewCandidateFinder` のロジックとテーブル（`watched_themes`）はコードとしては残置する**（F-005 の改修は本CRのスコープ外）。以下の記述は経緯として残す。

**Actor**: 本人
**前提条件**: 本人が「注目テーマ・セクター」を1件以上登録している
**関連要件**: F-008

#### 基本フロー
1. 本人が事前に「注目テーマ・セクター」（例: AI半導体、国策関連）を登録する
2. 本人が「新規投資候補」画面（UC-006と共通）にアクセスする
3. システムが画面上部のセクションに、登録済みテーマに合致するセクター・銘柄群の中から、財務健全性の基準（自己資本比率・ROE等）を満たす中長期銘柄を抽出して表示する
4. システムが該当銘柄と、小口購入額の目安（ポートフォリオ全体の1〜2%相当）を一覧表示する
5. 本人が気になる候補銘柄を選択すると、同一画面下部の重複チェック（UC-006）セクションに銘柄コードが自動入力され、詳細を確認できる

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| watched_theme（テーマ登録） | string | 必須（登録操作時のみ） | 本人が自由入力するテーマ・セクター名 |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| symbol_code / symbol_name | string | 銘柄基本情報 |
| matched_theme | string | 合致した登録済み注目テーマ名 |
| fundamental_summary | string | 財務健全性の簡易サマリ（例: 自己資本比率60%・ROE15%） |
| suggested_amount | decimal | 購入額の目安（保有評価額合計に対して1〜2%相当の小口） |
| nisa_recommended | boolean | NISA枠での購入推奨フラグ（ADR-0002） |
| nisa_recommended_reason | string | NISA推奨理由の一言サマリ（`nisa_recommended=false`の場合は空） |

#### 業務ルール
- 「注目テーマ・セクター」の登録・更新は本人が手動で行う。ニュース・社会的要因等からのテーマの自動発見・自動抽出は行わない（`requirements.md`のOUTスコープに従う。メタトレンド自体の予測はシステムの対象外）
- 財務健全性の基準は、ROE・自己資本比率・成長率（売上高成長率または営業利益成長率のいずれか）・営業利益率が一定水準を満たすこととする（2026-08-27、CHG-0005でUC-010と同一基準に統一。2026-09-06、CHG-0012／ADR-0011で営業利益率を4条件目として追加）。具体的な閾値は`docs/architecture/data-model.md`（Gate 3）で確定する
- 購入額の目安は保有評価額合計に対して1〜2%程度の小口を想定した簡易計算とする。具体的な計算式は`docs/architecture/data-model.md`（Gate 3）で確定する
- 本機能はあくまで候補の絞り込み支援であり、最終的な銘柄選定・購入判断は本人が行う
- 対象はJP株・US株の個別株のみとし、ETF・投資信託は対象外とする（`UC-002`業務ルール参照）
- 表示場所はUC-006（新規投資候補の重複チェック）と同一の「新規投資候補」画面の上部セクションとし、UC-008専用の独立したメニュー項目は設けない（`ui-guidelines.md`参照）
- **NISA推奨（`nisa_recommended`、ADR-0002）**: 財務健全性フィルタの基準に加えて、より高い健全性・成長性を示す候補（自己資本比率・ROE等の具体的な追加閾値は`docs/architecture/data-model.md`〔Gate 3〕で確定する）については`nisa_recommended=true`とし、中長期（5〜10年程度）の保有に適した銘柄として「NISA枠での購入」を推奨する理由を`nisa_recommended_reason`に表示する。この推奨はあくまで参考情報であり、実際にNISA枠・課税口座のどちらで購入するかは本人が判断する

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| 注目テーマが1件も登録されていない | - | 空状態として「注目テーマを登録してください」を表示し、テーマ登録導線を示す |
| 登録済みテーマに合致する条件を満たす銘柄が存在しない | - | 空状態として「条件に合致する候補銘柄はありません」を表示 |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-009: 取込後サマリーレポート

**Actor**: 本人
**前提条件**: CSV取込（UC-001）が完了し、国内株式・米国株式の保有銘柄データが存在する
**関連要件**: F-009

> **2026-09-17改訂（F-013/ADR-0014 D9）**: 従来の「優先度上位10件＋補足11〜20件レコメンド」（独自の非開示合成スコアで算出、ADR-0003）は、UC-013（ポートフォリオ分類ダッシュボード）の「分類俯瞰」セクションに**完全に置き換わった**。本UCは以後「取込を起点に本タブへ自動遷移する導線」「分類俯瞰セクションを載せるタブの入れ物」「タブからの恒常的な再表示（CHG-0008）」の3点を提供する画面としての役割に縮小し、独自の候補選定・優先度算出ロジックは持たない。詳細な分類ロジック・出力項目はUC-013を参照

#### 基本フロー
1. CSV取込（UC-001）が完了すると、システムが自動的に本レポート（分類俯瞰セクション）に遷移する
2. システムが直近スナップショットを対象にUC-013の分類俯瞰（バケツ分類・構成比・セクター偏りサマリ）を算出し、画面最上部に表示する（詳細はUC-013参照）
3. システムがバケツ件数の集計文（例:「整理検討3件・利確検討5件・買い増し候補2件、積立コア比率72%」）を`portfolio_headline`としてレポート冒頭に表示する
4. 本人が分類俯瞰内の銘柄行・バケツ見出しを選択すると、該当する既存画面（銘柄詳細UC-003・UC-004/UC-005/UC-010/UC-011/UC-012等）に遷移できる（UC-013フロー8参照）
5. **本人はグローバルナビゲーションの「サマリーレポート」タブから、直近の取込に基づくレポートをいつでも再表示できる（2026-09-05確定、CHG-0008）**。対象はスナップショットが存在する最新の取込バッチ1件のみとし、タブを開くたびに最新の指標で再計算する（過去の取込バッチを遡って閲覧する機能は提供しない）。取込完了直後（フロー1〜2）の表示内容と、タブから後日開いた表示内容は、指標の更新や保有状況の変化により一致しない場合がある

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| なし（UC-001の取込完了をトリガーに自動生成） | - | - | - |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| portfolio_headline | string | バケツ件数・構成比の集計文（2026-09-17改訂。旧：単一候補ハイライト文）。例:「整理検討3件・利確検討5件・買い増し候補2件、積立コア比率72%」 |
| generated_at | datetime | レポート生成日時 |
| classification | object | UC-013の分類俯瞰出力（`group_summary`/`hold_breakdown`/`buckets`/`sector_overweight_summary`/`new_entry_reference`）をそのまま埋め込む。詳細はUC-013出力参照 |

> `top_recommendations`/`supplementary_recommendations`（優先度上位10件＋補足11〜20件）は2026-09-17に廃止（上記参照）。

#### 業務ルール
- 分類俯瞰の算出ロジック・優先順位・並び順はすべてUC-013に従う。本UCで新しい判定条件・閾値・スコアリングは一切定義しない
- **永続化しない（2026-09-17改訂、ADR-0014 D9-2）**: 従来`import_summary_reports`/`import_summary_report_items`テーブルに書き込んでいたが、これらは書き込み専用で読み返す機能が存在しなかったため廃止した。取込完了時（`ImportCsvAction`）のプレースホルダー行作成も廃止した。2テーブルのスキーマ定義自体は残置（未使用）
- 投資信託・ETFの扱い、個別株の対象範囲はUC-013業務ルールに従う（本UC独自の対象範囲定義は持たない）
- **タブからの再表示（2026-09-05確定、CHG-0008）**: 「最新の取込バッチ」とは、スナップショット（`snapshots`）が存在する取込バッチのうち、取込日時が最も新しい1件を指す。取込自体は完了したがCSVパース失敗等でスナップショットが作成されなかったバッチは対象から除外し、その1つ前の成功バッチを最新として扱う。取込バッチが1件も存在しない場合は空状態（下記エラーケース参照）とする
- 本レポートはあくまで気づきを与える参考情報であり、自動発注は行わない（`requirements.md`のOUTスコープに従う）

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| 対象となる保有銘柄が存在しない（取込直後で個別株の保有がない等） | - | 空状態として「分類対象の保有銘柄がありません」を表示（UC-013エラーケース準拠） |
| タブから開いた際、スナップショットを持つ取込バッチが1件も存在しない（CSV取込を一度も実行していない） | - | 「まだCSVの取込がありません」を表示し、CSV取込画面（UC-001）への導線を示す |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-010: 既存保有株の買い増しタイミングレコメンド

**Actor**: 本人
**前提条件**: 保有銘柄のテクニカル指標が計算済み（UC-003の計算処理に依存）
**関連要件**: F-010

#### 基本フロー
1. 本人が売買シグナル画面（買い増し候補セクション）にアクセスする
2. システムが押し目買いシグナルが1件以上発生している保有銘柄を抽出する
3. システムが各銘柄にファンダメンタルズ健全性フィルタ（ROE・自己資本比率・成長率・営業利益率）を適用し、基準を満たさない銘柄を除外する
4. システムが分割買い下がりの指値提案（例: 現在値／-7%地点／-15%地点で3分割）を表示する
5. システムが各銘柄について、なぜ買い増しを検討すべきかを平易な一言サマリとして一覧に表示する
6. 本人が銘柄をクリックすると、銘柄詳細（UC-003）に遷移し、判定の背景・全指標を確認できる

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| なし | - | - | - |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| symbol_code / symbol_name | string | 銘柄基本情報 |
| current_price | decimal | 現在値 |
| market_value | decimal | 評価額（保有数量 × 現在値）。US株はCSVヘッダーの参考為替レートで円換算済みの `current_price` を用いるため円建て（2026-09-05追加、CHG-0011）。一覧では銘柄名の右隣に表示する |
| unrealized_gain_rate | decimal | 含み益率（%）。マイナスも含む |
| buy_signal_types | list | 発生している買い増しシグナル種別のリスト |
| buy_signal_reason_summary | string | 判定根拠の一言サマリ（例: "RSIが28から34へ反発しました、終値がボリンジャーバンド下限を下回りました"） |
| fundamental_status | string | `passed`（健全性フィルタを満たす）/ `unavailable`（ファンダメンタルズ指標が未取得のため判定不可）。フィルタを満たさない銘柄（`failed`相当）は一覧から除外されるため出力されない |
| fundamental_summary | string | ファンダメンタルズ健全性の一言サマリ（例: "ROE15.2%・自己資本比率58.0%・営業利益成長率+12.3%・営業利益率18.3%"）。`fundamental_status=unavailable`の場合は取得不可である旨を表示する。**成長率表示の優先順位（2026-09-21改訂、ADR-0015 D1/D2、`/review`3回目の指摘）**: 単年度成長率（売上高・営業利益いずれか）がプラスならそれを表示、なければ直近3期平均成長率（D2救済の根拠）がプラスならそれを「3期平均◯◯成長率」として表示、それも無くROE・自己資本比率がD1救済閾値（ROE≧15%かつ自己資本比率≧50%）を満たす場合は成長率を表示せず「ROE◯%・自己資本比率◯%と財務健全性が高いため合格しています」と表示する（マイナスの単年度成長率をそのまま合格根拠として誤って見せない） |
| nisa_recommended | boolean | 中長期保有前提のNISA枠での購入推奨可否 |
| nisa_recommended_reason | string | NISA枠推奨理由の一言サマリ（`nisa_recommended=true`の場合のみ） |
| suggested_amount | decimal | 追加投資額の目安（ポートフォリオ評価総額に対する小口の目安） |
| split_buy_down_suggestion | list | 分割買い下がりの提案（価格帯・数量） |
| criteria | object | 判定チェックリスト（下記業務ルール参照）。UC-004の `criteria` と同一構造（`technical` 7項目・`fundamental` 4項目〔2026-09-06、CHG-0012で3→4〕・`summary`） |

#### 業務ルール
- 対象はJP株・US株の個別株のみとし、ETF・投資信託は対象外とする（`UC-002`業務ルール参照）
- 買い増しシグナル種別（`signal_type`）は以下7種とする。UC-004の7種と面対称の設計とし、値は`docs/architecture/data-model.md`の初期パラメータ値と同様、叩き台として実装時に確定する（ADR-0007）
  - `rsi_oversold_rebound`（RSI低水準からの反発。初期値: RSI30以下からの反発）
  - `macd_golden_cross`（MACDゴールデンクロス）
  - `bollinger_oversold`（ボリンジャーバンド下限付近での売られすぎ）
  - `week52_low_proximity`（52週安値圏。初期値: 52週安値から+10%以内）
  - `ma_deviation_oversold`（移動平均線からの下方乖離。初期値: MA20から-10%以上の乖離）
  - `volume_spike_rebound`（出来高急増を伴う反発。初期値: 当該週の出来高が20週平均の1.5倍以上、かつ株価が前週比上昇）
  - `peg_undervalued`（PEGレシオによる割安化。初期値: PEGレシオ1.0以下）。**成長率〔売上高・営業利益の高い方〕が低い（5%以下、暫定閾値）銘柄は本シグナルを判定対象から除外し、PER≦15かつ配当利回り≧3%（暫定閾値）の絶対閾値判定に置き換える（2026-09-19追加、ADR-0015 D3。判定は成長率データのみを参照し、銘柄のセクター分類には依存しない）**
- ファンダメンタルズ健全性フィルタは、ROE・自己資本比率・成長率（売上高成長率または営業利益成長率のいずれか）・営業利益率（2026-09-06、CHG-0012／ADR-0011で追加）が一定水準を満たす銘柄のみを対象とする。具体的な閾値は`docs/architecture/data-model.md`（Gate 3）で確定するが、UC-009の新規投資候補フィルタと同一値を用いて一貫性を保つ。**成長率項目は`FundamentalHealthEvaluator`を共有するため、UC-004業務ルールの「直近3〜5期平均成長率>0%」OR救済分岐（ADR-0015 D2）の影響を自動的に受ける**
- **全シグナル共通の前提条件（2026-08-23追加）**: 本機能の意図は「健全で好調だった銘柄が、市場全体・セクター全体の調整で一時的に下げた場面」を拾うことであり、長期低迷銘柄や個別要因で下落している銘柄を拾う設計ではない。この意図を担保するため、7種のシグナルいずれも以下3条件を満たした場合のみ検出対象とする（具体的な閾値は`docs/architecture/data-model.md`〔Gate 3〕で確定するが、初期値は以下を叩き台とする）
  - **直前まで好調だった証拠**: 直近13週以内に52週高値（`week52_high`）の-15%以内に到達していたこと。52週高値に一度も接近せず下げ続けている長期低迷銘柄を除外する目的
  - **市場全体・セクター全体の連れ安であることの確認（2026-09-19改訂、ADR-0015 D4）**: `relative_strength_vs_sector`（対セクター相対力）が算出済みの銘柄はそちらを、未算出（null）の銘柄は従来通り`relative_strength_vs_market`（対市場）を用い、-5pt以上であること。個別銘柄固有の悪材料（業績下方修正等）により市場・セクター平均を大きく下回って下落している銘柄は対象外とする
  - **中期トレンドが健在であることの確認（2026-09-19追加、ADR-0015 D5）**: 週足MA75の傾きが上向き（直近値が13週前の値を上回る）であること。下降トレンドが継続中の銘柄を「押し目」として誤検出しないための条件
- ファンダメンタルズ指標が未取得（JP株・US株いずれも外部データ取得に失敗した銘柄）の場合は`fundamental_status=unavailable`として一覧に含める（除外はしない）。US株はFinnhub（ADR-0009）をデータソースとしており、通常時はJP株と同様にファンダメンタルズ健全性フィルタで判定する
- 利確シグナル（UC-004）が同時に成立している銘柄は対象外とする（同一銘柄に「売れ」と「買え」を同時に提示しないため）
- **NISA区分は除外要因にしない（ADR-0002/ADR-0007）**: 利確シグナル一覧（UC-004）と異なり、NISA区分の保有であっても買い増し対象から除外しない。ファンダメンタルズ健全性フィルタより高い基準（ROE・自己資本比率がより高い水準）を満たす銘柄には、中長期保有前提のNISA枠での購入推奨（`nisa_recommended`）を付与する。ただしNISA枠の年間/生涯の残量情報はCSVに含まれないため、推奨は残量を考慮しない参考情報にとどまる
- 分割買い下がりの提案は固定ルールではなく例示であり、具体的な閾値・比率のパラメータ化は`docs/architecture/data-model.md`（Gate 3）で確定する
- 一覧の並び順は、判定の透明性を優先し合成スコアリングを用いない。ファンダメンタルズ健全性の状態（`passed`を`unavailable`より優先）→買い増しシグナルの件数→含み益率の低い順、という単純な複数キーでソートする（ADR-0007。ADR-0003が定めるF-009の合成スコアリング方針とは別の設計とする）
- 一覧では技術的なシグナル種別バッジに加えて、`buy_signal_reason_summary`を1行で表示し、判断軸が一目でわかるようにする。全指標の内訳・判定の詳細背景は一覧には出さず、銘柄詳細（UC-003）に集約する（UC-004と同じ方針）
- **判定チェックリスト（2026-08-29追加、CHG-0007）**: UC-004と同じ方針で、一覧の各銘柄行に判定項目の「基準点」「実測値」「達成状態」を並べた表（`criteria`）を表示する。表示のみの機能で `buy_signals` の判定・永続化ロジックには影響しない
  - **テクニカル7項目**（買い増しを後押しする方向を「達成」とする。UC-004の7項目と面対称）: ①RSI（≦30）②52週安値からの距離（≦+10%）③ボリンジャーバンド下限乖離率（≦0%）④MACD−シグナル線（>0）⑤MA20乖離率（≦-10%）⑥PEGレシオ（≦1.0）⑦出来高倍率〔当該週÷20週平均〕（≧1.5倍）。基準値は `docs/architecture/data-model.md` の確定済み閾値をそのまま用いる。**成長率が低い（5%以下、暫定閾値）銘柄はPEGレシオ項目（⑥）を判定対象から除外し、PER≦15かつ配当利回り≧3%の絶対閾値判定に置き換える（2026-09-19改訂、ADR-0015 D3。セクター分類には依存しない。UC-004業務ルール参照）**
  - **財務健全性4項目**: UC-004と同一（ROE≧10%／自己資本比率≧40%／成長率>0%／営業利益率≧10%。2026-09-06、CHG-0012で3→4項目）。買い増し側では財務健全性は「後押し材料」であり向きが揃うが、UC-004と表示構造を合わせるため同様に別グループとして集計する。**成長率項目のOR救済分岐（ADR-0015 D2）はUC-004と共有のため本UCにも適用される**
  - 達成状態の4値・`near` バッファ・`unavailable` の扱い・`ui-guidelines.md` 方針の例外扱いはUC-004と同一

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| 対象銘柄が存在しない | - | 空状態として「買い増しを検討できる押し目銘柄はありません」を表示 |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-011: 整理検討（含み損）候補一覧

**Actor**: 本人
**前提条件**: 保有銘柄のテクニカル指標が計算済み（UC-003の計算処理に依存）
**関連要件**: F-011

#### 基本フロー
1. 本人が売買シグナル画面（整理検討セクション）にアクセスする
2. システムが含み損が整理検討ライン（叩き台: -20%）を下回る個別株を抽出する
3. システムが各銘柄について、復帰に必要な上昇率・含み損の実額とポートフォリオ比・観測以来の連続保有週数を算出する
4. システムがファンダメンタルズ健全性フィルタ（ROE・自己資本比率・成長率・営業利益率）の合否を判定する（不合格でも一覧から除外しない）
5. システムが整理を後押しする判定チェックリスト（テクニカル7項目＋財務健全性4項目〔保留材料〕）を表示する
6. システムが各銘柄について、なぜ整理を検討すべきか（またはまだ保留してよいか）を平易な一言サマリとして一覧に表示する
7. 本人が銘柄をクリックすると、銘柄詳細（UC-003）に遷移し、判定の背景・全指標を確認できる

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| なし | - | - | - |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| symbol_code / symbol_name | string | 銘柄基本情報 |
| unrealized_gain_rate | decimal | 含み益率（%）。本UCの対象は常にマイナス |
| unrealized_gain_amount | decimal | 含み損の実額（円）。US株は取込時に参考為替レートで円換算済み（UC-001業務ルール） |
| recovery_required_rate | decimal | 復帰に必要な上昇率（%）。現在値を取得単価まで戻すのに必要な上昇率。含み益率 r%（負）に対し `(1 / (1 + r/100) - 1) * 100` で算出 |
| portfolio_loss_share | decimal | 含み損額の絶対値がポートフォリオ評価総額に占める割合（%） |
| continuous_holding_weeks | int | 観測以来の連続保有週数（推定）。対象銘柄が直近スナップショットから連続して出現している `holding_snapshots` の数 |
| continuous_holding_weeks_is_truncated | boolean | 最古のスナップショットまで連続して出現していた場合 true（実際の保有期間はそれ以上）。画面は「N週以上（観測開始まで遡及）」と表示する |
| fundamental_status | string | `passed`（健全性フィルタを満たす）/ `unavailable`（ファンダメンタルズ指標が未取得）/ `failed`（基準割れ）。UC-010と異なり `failed` も出力する |
| fundamental_summary | string | ファンダメンタルズ健全性の一言サマリ。`failed` の場合は基準を割った項目を含める（例: "ROE4.2%（基準10%未満）・自己資本比率28.0%（基準40%未満）・営業利益率7.8%（基準10%未満）"）。営業利益率とROEは注記の文言・閾値がどちらも「（基準10%未満）」で同じになるため、必ず指標名とセットで並べる |
| rebound_buy_signal_count | int | 発生中の押し目買いシグナル（`buy_signals`、UC-010）の件数 |
| rebound_buy_signal_present | boolean | `rebound_buy_signal_count >= 1`。true の場合、一覧に「押し目買いシグナル発生中・買い増し候補にも掲載」と表示する |
| loss_review_reason_summary | string | 判定根拠の一言サマリ（例: "含み損-38%、52週高値から-45%まで下落、相対力-12で市場を継続的に下回っており、財務健全性も基準割れ。反発の兆しは出ていません"）。整理を保留してよい銘柄には、なぜ保留してよいか（押し目シグナルの発生・財務健全等）を示す文言を表示する |
| criteria | object | 判定チェックリスト（下記業務ルール参照）。UC-004の `criteria` と同一構造（`technical` 7項目・`fundamental` 4項目〔2026-09-06、CHG-0012で3→4〕・`summary`） |

#### 業務ルール
- 対象はJP株・US株の個別株のみとし、ETF・投資信託は対象外とする（`UC-002`業務ルール参照）
- 対象抽出は直近の週次スナップショットの含み益率が整理検討ライン（叩き台: -20%）を下回る銘柄とする。閾値は`docs/architecture/data-model.md`（Gate 3）で確定する
- **本機能は「整理（売却検討）の踏ん切りをつけるための判断材料の提示」であり、機械的な損切り指示・分割売却の指値提案・自動発注・ナンピン推奨は行わない（`requirements.md` 2章、ADR-0010）**。画面上に「自動売却は行いません。最終判断は本人が行います」を明示する
- `recovery_required_rate` は含み益率がプラスの銘柄は対象外のため常に正の値になる。「この上昇率を、この銘柄が過去どれくらいの期間で達成し得たか」を本人が考える材料として提示する
- **ファンダメンタルズ健全性フィルタが不合格（`failed`）の銘柄も一覧から除外しない（UC-010とは逆）**。財務の毀損は整理を後押しする材料であり、隠すと本機能の意味がなくなるため。`fundamental_status` には `passed` / `unavailable` / `failed` の3値を出力する
- **押し目買いシグナル（`buy_signals`、UC-010）が発生している銘柄も除外しない**。一覧に含めたうえで「押し目買いシグナル発生中・買い増し候補にも掲載」と明示し、並び順で最後尾に回す。「切れ」と「買え」を矛盾提示しないための順序制御であって、情報の抑制はしない
- 一覧の並び順は、判定の透明性を優先し合成スコアリングを用いない。①押し目買いシグナルなし→あり、②ファンダ状態（`failed`→`unavailable`→`passed`）、③整理テクニカル該当数の多い順、④含み損率の深い順、という単純な複数キーでソートする（ADR-0010。ADR-0003が定めるF-009の合成スコアリング方針とは別の設計とする）
- 一覧では、`loss_review_reason_summary`を1行で表示し、判断軸が一目でわかるようにする。全指標の内訳・判定の詳細背景は一覧には出さず、銘柄詳細（UC-003）に集約する（UC-004/UC-010と同じ方針）
- **判定チェックリスト（`criteria`）**: UC-004/UC-010と同じ方針で、一覧の各銘柄行に判定項目の「基準点」「実測値」「達成状態」を並べた表を表示する。表示のみの機能で、対象抽出・並び順のロジックには影響しない
  - **テクニカル7項目**（整理を後押しする方向を「達成」とする。UC-004の7項目と面対称）: ①含み損率（≦整理検討ライン、叩き台-20%）②52週高値からの下落率（≦-30%、叩き台）③52週安値からの距離（≦+10%）④MA75乖離率（≦-10%）⑤MACD−シグナル線（<0）⑥相対力〔対市場〕（≦-5）⑦押し目買いシグナル件数（=0件）。基準値のうち①②⑦は`docs/architecture/data-model.md`（Gate 3）で新規確定する叩き台、③④⑤⑥は既存の確定済み閾値（`BuySignalDeterminationService`等）をそのまま可視化するもので新しい基準値は設けない
  - **財務健全性4項目**（2026-09-06、CHG-0012／ADR-0011で3→4項目。営業利益率を追加）: 閾値の値はUC-004/UC-010と同一（ROE 10%／自己資本比率 40%／成長率 0%／営業利益率 10%）。成長率項目のOR救済分岐（ADR-0015 D2）は`FundamentalHealthEvaluator`共有のため本UCにも適用される（整理検討では判定が反転するため、救済により`unmet`〔健全〕側に動く銘柄が増える方向）。**整理検討テーブルでは判定の向きを反転**し、「基準割れ（＝投資根拠の毀損）」を「達成（`met`）」とする（2026-09-06、ADR-0010 D6 改訂。営業利益率も同じ反転フラグに乗せ、10%未満で `met`）。整理検討テーブルの判定チェックリストは「テクニカル7項目・財務4項目のいずれも、整理を後押しする事実が成立していれば `met`」で統一する。健全な財務は `unmet` として扱い、実測値（例「ROE 15.2%」「営業利益率 18.3%」）は表示する。テクニカル7項目とは集計を分け、サマリは「投資根拠の毀損 ◯/4」と表示する。「まだ売らなくてよい（保留材料）」は `fundamental_status`（`passed`/`unavailable`/`failed`）・`fundamental_summary`・`loss_review_reason_summary` の散文で伝える
  - **達成状態**: 各項目は「達成（`met`）」「あと一歩（`near`）」「未達（`unmet`）」「データなし（`unavailable`）」の4値。`near` バッファ・基準値0の項目の扱い・`unavailable` の扱い・`ui-guidelines.md` テーブル方針の例外扱いはUC-004と同一。**チップの配色のみ整理検討テーブル専用**とし、達成＝赤系／あと一歩＝淡い赤／未達・データなし＝グレーとする（利確検討・買い増し候補テーブルの緑チップと視覚的に区別し、1テーブル内に赤と緑を混在させない。`ui-guidelines.md` 参照）
- **推定保有期間（`continuous_holding_weeks`）**: 対象銘柄が直近スナップショットから連続して何週分の `holding_snapshots` に出現しているかで算出する。楽天証券CSVに取得日が含まれないための代替であり、正確な保有期間ではない。最古のスナップショットまで連続して出現していた場合は `continuous_holding_weeks_is_truncated=true` とし、画面上は「N週以上（観測開始まで遡及）」と表示する。取込回数が少ない時期は情報量が限られる旨を画面に注記する
- **NISA区分の扱い**: 含み損は損益通算・非課税メリットの観点が利確（UC-004）とは逆になる（NISA口座の含み損は損益通算できない）。本フェーズではNISA区分による対象銘柄の絞り込み・除外は行わず、全口座区分の含み損銘柄を一覧表示する。画面上に「NISA区分の含み損は損益通算できない点に留意」の注記を表示する
- 対象銘柄の指標算出に必要なデータが取得できない場合、該当のチェックリスト項目のみ `unavailable`（値「—」）と表示し、他の項目・行の表示は継続する

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| 対象銘柄が存在しない | - | 空状態として「整理検討が必要な含み損銘柄はありません」を表示 |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-012: お気に入り未保有銘柄ウォッチリスト

**Actor**: 本人
**前提条件**: 楽天証券Web画面からお気に入り銘柄リストCSVをエクスポート済み。国内株式・米国株式の保有CSV取込（UC-001）が少なくとも1回完了している（同セクター保有比率・保有済み判定の基準に直近スナップショットを用いるため）
**関連要件**: F-012（UC-006／UC-008 を統合・刷新、ADR-0013）

#### 基本フロー（CSV取込）
1. 本人が「新規投資候補」画面（`/candidate-check`）上部の取込バーで、お気に入り銘柄CSVを選択してアップロードする
2. システムがCSVをパースする（CP932デコード、改行はCRLF、1行目のメタ行をスキップ、`STK`＝日本株・`USS`＝米国株のみ対象、`CFD`等その他の市場区分の行はスキップ）
3. システムが各銘柄を銘柄マスタ（`holdings`）に find-or-create し、ウォッチリスト（`watchlist_items`）に登録する（既存の銘柄は再利用し重複を作らない。フォルダ名・取引所ラベルを更新する。`is_starred` は保持する）
4. 今回のCSVに含まれない既存のウォッチリスト銘柄は削除しない（`last_seen_in_csv_at` を更新しないことで「楽天側のお気に入りからは外れている」ことを表示できる）
5. システムが取込完了後、未保有のウォッチリスト銘柄に対する一括更新（下記「基本フロー（一括更新）」のフロー2以降）を自動でキュー投入する
6. システムが取込結果（登録件数・スキップ件数）を表示する

#### 基本フロー（一括更新）
1. 本人が取込バーの「一括更新」ボタンを押す
2. システムが更新ジョブをキューに投入し、即座に画面へ戻る（同期的に待たせない）。実行状態を `watchlist_refresh_runs` に記録する
3. ワーカーが、直近スナップショットに保有として存在しないウォッチリスト銘柄を対象に、1銘柄ずつ次を実行する
   - 週足株価履歴を取得（JP株: Yahoo Finance相当、US株: Yahoo Finance相当）
   - JP株は業種分類・財務諸表（J-Quants）、US株はファンダメンタルズ指標（Finnhub）を取得
   - テクニカル指標（`technical_indicators`）・ファンダメンタルズ指標（`fundamental_indicators`）・過去業績（`financial_statements`、JP株のみ）を UPSERT する
   - 押し目買いシグナル（`BuySignalDeterminationService`）を判定し `watchlist_buy_signals` に保存する（銘柄単位で delete → 再作成）
   - 1銘柄終えるごとに `watchlist_refresh_runs.processed_count` を加算する
   - 銘柄単位の取得失敗はログに記録してスキップし、その銘柄は「取得不可」表示にフォールバックする（他の銘柄の処理は継続する）
4. 画面は実行中、`watchlist_refresh_runs` をポーリングして「更新中 42/150」のように進捗を表示する
5. 完了後、一覧が最新の指標・シグナルで再描画される
6. **保有銘柄の指標は本フローの対象外**（週次CSV取込 UC-001 フロー7で更新済みのため）。`signals` / `buy_signals`（UC-004／UC-010）テーブルには一切書き込まない

#### 基本フロー（一覧の閲覧・★お気に入り）
1. 本人が「新規投資候補」画面にアクセスする
2. システムが未保有のウォッチリスト銘柄を一覧表示する（保有済み銘柄は一覧から除外）
3. システムが各銘柄について、現在値・52週レンジ内位置・同セクター保有比率・押し目買いシグナル件数・財務健全性判定・主要指標（RSI／PER／PBR／ROE／自己資本比率／営業利益率）・判定チェックリスト（UC-010と同一構造）を表示する
4. システムが一覧を透明マルチキーでソートする（下記業務ルール）
5. 本人がフォルダで絞り込む／★お気に入りのみ表示するフィルタを操作できる
6. 本人が銘柄行を展開すると、過去の業績推移・分散への影響コメント・ウォッチステータス／メモ（記録と履歴）を確認・入力できる（UC-006 相当）
7. 本人が★アイコンをクリックすると、その銘柄の `watchlist_items.is_starred` が反転する（お気に入りの手動登録・解除）
8. 本人が銘柄名をクリックすると銘柄詳細（UC-003）に遷移する

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| favorites_csv_file | ファイル（CSV） | 必須（取込操作時のみ） | 拡張子`.csv`、最大サイズ5MB。1行目メタ行を除き有効行が1件も無い場合は取込エラー |
| folder_filter | string | 任意 | CSVに存在するフォルダ名のいずれか |
| starred_only | boolean | 任意 | true のとき `is_starred=true` の銘柄のみ表示 |
| watch_status | string | 任意 | `様子見`/`買い時`/`次回購入候補`/`リバランス対象`のいずれか（行展開時） |
| watch_memo | text | 任意 | 最大2000文字（行展開時） |

#### 出力（一覧の各行）
| 項目 | 型 | 説明 |
|---|---|---|
| symbol_code / symbol_name | string | 銘柄基本情報 |
| market | string | `jp` / `us` |
| folder_name | string | お気に入りCSVのフォルダ名（本人のテーマ分類） |
| is_starred | boolean | 画面上で手動登録された★お気に入りか |
| in_rakuten_favorites | boolean | 直近のお気に入りCSVに含まれていたか（false なら「楽天側で解除済み」バッジ） |
| current_price | decimal | 現在値。US株は取込時の指標取得で扱う現地通貨建て（判定チェックリストの通貨単位整合は UC-004 と同じ扱い、CHG-0010） |
| week52_range_position | decimal | 52週レンジ内の位置（0〜1）。`(現在値 − week52_low) ÷ (week52_high − week52_low)` |
| overlap_rate | decimal | 対象銘柄と同一セクターの既存保有比率（UC-006 の `overlap_rate` と同一算出。`SectorAllocationCalculator` 流用） |
| diversification_comment | string | 分散影響に関するコメント（UC-006 と同一。行展開時に表示） |
| rebound_buy_signal_count | int | 発生中の押し目買いシグナル（`watchlist_buy_signals`）の件数 |
| fundamental_status | string | `passed` / `unavailable` / `failed`（財務健全性フィルタの合否） |
| fundamental_summary | string | ファンダメンタルズ健全性の一言サマリ（UC-010 と同一形式） |
| suggested_amount | decimal | 小口購入額の目安（保有評価額合計の1%。UC-008 から引き継ぎ） |
| nisa_recommended / nisa_recommended_reason | boolean / string | NISA枠購入推奨フラグと理由（UC-008 と同一基準、ADR-0002） |
| rsi / per / pbr / roe / equity_ratio / operating_margin | decimal等 | 主要指標（未取得は「—」） |
| criteria | object | 判定チェックリスト。UC-010（買い増し候補）の `criteria` と同一構造・同一配色（`technical` 7項目・`fundamental` 4項目・`summary`）。`SignalCriteriaEvaluator::evaluateBuy()` を流用する |
| historical_performance | list | 直近5期分の決算期・売上高・営業利益・前年比増減（行展開時。UC-006 と同一。JP株のみ） |
| watch_status / watch_memo_history | string / list | ウォッチステータスの最新値と履歴（行展開時。UC-006 と同一） |

#### 出力（取込・更新バー）
| 項目 | 型 | 説明 |
|---|---|---|
| last_refreshed_at | timestamp | 直近の一括更新の完了日時 |
| refresh_status | string | `idle` / `queued` / `processing` / `completed` / `failed` |
| refresh_progress | string | 実行中の進捗（例: `42 / 150`） |
| watchlist_count / unheld_count | int | ウォッチリスト総数と、うち未保有の件数 |

#### 業務ルール
- 対象はJP株・US株の個別株のみとする。お気に入りCSVの `CFD` 行、および `USS` に含まれる実質ETF（例: SPYD/HDV/VYM）はファンダメンタルズが疎になり判定チェックリストの財務項目が `unavailable` に寄るが、除外はしない（実測後に除外要否を判断する）
- **一覧に表示するのは未保有銘柄のみ**。「未保有」＝直近の週次スナップショット（`holding_snapshots`）に当該 `holding_id` が存在しない、と定義する。保有済みになった銘柄は自動的に一覧から外れる（`watchlist_items` の行は消さない）
- **候補の絞り込みはしない**。押し目買いシグナル・財務健全性フィルタは表示項目とソートキーに用いるだけで、これらを満たさない銘柄も一覧に残す（ADR-0013 D4。押し目買いシグナルの共通前提〔直近13週で52週高値の85%以内〕は保有銘柄の買い増し向けに設計されており、高値更新中の優良銘柄を取りこぼすため、新規候補では絞り込み条件に使わない）
- 一覧の並び順は合成スコアリングを用いず、透明な複数キーでソートする（ADR-0013 D4、ADR-0010 D8 と同方針）: ①押し目買いシグナル件数の多い順、②財務状態（`passed`→`unavailable`→`failed`）、③同セクター保有比率の低い順（分散に効く順）、④52週レンジ内位置の低い順（高値から離れている順）
- **判定チェックリスト（`criteria`）**: UC-010（買い増し候補）の判定チェックリストをそのまま流用する（`SignalCriteriaEvaluator::evaluateBuy()`）。配色も UC-010 と同一（達成＝緑系）。整理検討テーブル（UC-011）の赤の単一極性とは別扱い
- **同セクター保有比率（`overlap_rate`）・分散コメント**: UC-006 と同一で `SectorAllocationCalculator` の `allocation_rate` / `allocation_status` を流用する。対象銘柄のセクター名に一致する保有セクターが無い場合は `overlap_rate=0`
- **ウォッチステータス・メモ**: UC-006 と同一運用（`watch_records` に追記のみ、最新1件を現在ステータスとして表示、銘柄コード単位で履歴表示）。行展開UIから記録する
- **★お気に入り（`is_starred`）**: 画面上で本人が任意に付け外しする。`watch_records`（追記のみで「外す」が表現できない）とは別の独立した boolean として `watchlist_items` に持つ。楽天証券側のお気に入り（CSV由来）とは独立で、CSVから消えても `is_starred` は保持される
- **データ取得タイミング**: お気に入りCSVアップロード時に自動で、または「一括更新」ボタンで手動で、未保有ウォッチリスト銘柄の指標を取得する。保有銘柄の指標は週次CSV取込（UC-001）で更新されるため本機能の対象外。取得は約100〜150銘柄への外部API呼び出しを伴いFinnhub無料枠（60req/分）の律速で数分かかるため、キュー（非同期ジョブ）で実行し画面に進捗を表示する
- **買いシグナルの保存先**: 未保有銘柄は `holding_snapshots` 行を持たないため、`holding_snapshot_id` 外部キーを持つ `buy_signals` を流用できない。`holding_id` をキーとする専用テーブル `watchlist_buy_signals` に保存する（`buy_signals` とは値域が同一。ADR-0013 D3）
- **`signals` / `buy_signals` テーブルには一切書き込まない**。これらは保有銘柄の週次シグナル（UC-004／UC-010）の領分であり、本機能はウォッチ専用テーブルで完結する
- 対象銘柄の指標算出に必要なデータが取得できない場合、該当項目のみ「取得不可（—）」と表示し、他の項目・行の表示は継続する（UC-006 業務ルールと同じ方針、ADR-0009 D5）
- 小口購入額の目安（`suggested_amount`）・NISA推奨（`nisa_recommended`）は UC-008 と同一基準・同一算出（`docs/architecture/data-model.md` の「保留・確定が必要な初期パラメータ値」表を参照）
- CSV再アップロード時は追加のみ（前回あって今回無い銘柄も `watchlist_items` に残す）。楽天側でお気に入りから外した銘柄は `in_rakuten_favorites=false` として一覧に残り、本人が行から★解除・ウォッチリスト削除を任意に行える
- 本機能は候補の絞り込み支援であり、最終的な銘柄選定・購入判断は本人が行う（`requirements.md` 1章）

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| お気に入りCSVの形式が不正（デコード不可／有効行なし） | 422 | お気に入り銘柄CSVの形式を確認してください |
| ウォッチメモが2000文字超 | 422 | メモは2000文字以内で入力してください |
| ウォッチリストが空（CSV未取込） | - | 空状態として「お気に入り銘柄CSVを取り込んでください」を表示し、取込導線を示す |
| 一括更新の実行中に再度「一括更新」を押した | - | 「更新処理を実行中です」を表示し、二重実行しない |
| 未保有のウォッチリスト銘柄が0件（全て保有済み） | - | 空状態として「未保有のお気に入り銘柄はありません」を表示 |

#### 権限
- 本人: 実行可（他Actorなし）

---

### UC-013: ポートフォリオ分類ダッシュボード

**Actor**: 本人
**前提条件**: CSV取込（UC-001）が1回以上完了し、直近の週次スナップショットに保有銘柄が存在する
**関連要件**: F-013（ADR-0014）

> **位置づけ**: UC-004（利確検討）/ UC-005（セクター配分）/ UC-010（買い増し候補）/ UC-011（整理検討）/ UC-012（ウォッチリスト）の抽出結果を**銘柄起点で1つのバケツに束ね直し**、ポートフォリオ全体を「減らす / 保つ / 増やす」の構成比とともに俯瞰する画面。新しい抽出条件・閾値は設けず、既存UCの対象抽出ロジックをそのまま再投影する（ADR-0014 D2）。第1段階は表示レイヤー完結（DBスキーマ変更なし・永続化なし）。先週からの遷移表示は第2段階（別CR）。UC-009（取込後サマリーレポート）の旧・上位10〜20件レコメンドを置き換える画面でもある（2026-09-17確定、ADR-0014 D9）。

#### 基本フロー
1. 本人がグローバルナビの「サマリーレポート」タブを開く
2. システムが画面最上部に「分類俯瞰」セクションを表示する（**2026-09-17確定: UC-009の旧・上位10〜20件レコメンドはこのセクションに完全に置き換わり、併存しない**）
3. システムが直近スナップショットの各保有銘柄を、下記の優先順位でちょうど1つのバケツに分類する
4. システムが3大分類（減らす / 保つ / 増やす）ごとに、評価額（`market_value`）構成比・銘柄数・銘柄リストを表示する。各バケツ内は下記のバケツ内ソート順で並べ、**件数上限は設けず該当銘柄を全件表示する**（ダイジェスト性は上限カットではなく並び順で担保する）
5. システムが「保つ」の内訳（積立・インデックスコア / キープ）を明示し、積立コア比率を数値で示す
6. システムがセクター偏りサマリ（UC-005 の `allocation_status` 要約）を併置し、偏り警告セクターに属する銘柄行に「偏り警告セクター」バッジを付す
7. システムが「キープ」の各行に簡易ヘルスライン（含み益率・RSI帯・財務健全性の合否・相対力〔対市場〕の符号）と「要観察（`hold_watch`）」フラグを表示する
8. 本人が銘柄行をクリックすると銘柄詳細（UC-003）へ、バケツ見出しのリンクから対応する既存一覧（UC-004/UC-010/UC-011/UC-005/UC-012）へ遷移できる

#### 入力
| 項目 | 型 | 必須 | バリデーション |
|---|---|---|---|
| bucket_filter（フィルタ） | string | 任意 | バケツ識別子のいずれか（表示を1バケツに絞る） |

#### 出力
| 項目 | 型 | 説明 |
|---|---|---|
| classified_at | datetime | 分類の基準となったスナップショットの取込日時 |
| group_summary | list | 3大分類（`reduce` / `hold` / `increase`）ごとの `market_value_total`・`allocation_rate`（評価額構成比%）・`holding_count` |
| hold_breakdown | object | 「保つ」の内訳。`core_accumulation`（積立・インデックスコア）と `hold`（キープ）それぞれの `market_value_total`・`allocation_rate`・`holding_count` |
| buckets | list | バケツごとの銘柄リスト（下記バケツ内ソート順で並び済み）。各要素は `bucket`（下記6種）・`group`（`reduce`/`hold`/`increase`）・`holdings`（銘柄配列） |
| holding（buckets[].holdings[] の各要素） | object | `symbol_code` / `symbol_name` / `market` / `instrument_type` / `market_value` / `unrealized_gain_rate` / `bucket_reason`（なぜこのバケツかの一言評価。`SignalCriteriaEvaluator`の達成度データから機械生成する簡潔な理由文。例: 「技術3/7達成・RSI72が基準70を超過」「含み損-38%・整理検討ライン超過」） / `also_matched`（該当したが優先順位で採らなかったバケツのリスト。行内注記に使う） / `overweight_sector`（偏り警告セクター所属フラグ） / `hold_watch`（`hold` バケツのみ: 要観察フラグ） / `health_line`（`hold` バケツのみ: RSI・財務合否・相対力符号の簡易サマリ文字列） |
| sector_overweight_summary | list | UC-005 由来のセクター偏りサマリ。各要素は `sector_name` / `allocation_rate` / `allocation_status`（`健全`/`やや偏り`/`偏り警告`）。配分率の超過幅が大きい順に並ぶ |
| new_entry_reference | list | UC-012 ウォッチリストの未保有銘柄（`new_entry` バケツ。評価額構成比の分母・分子には含めない参考表示。UC-012既存の並び順のまま。`symbol_code` / `symbol_name` / `folder_name` / `rebound_buy_signal_count` / `fundamental_status`） |

#### 業務ルール
- **バケツ定義（ADR-0014 D2）**: 以下6種。所属条件はいずれも既存UCの対象抽出条件の再利用であり、本UCで新しい閾値・判定ロジックを定義しない
  - `core_accumulation`（保つ）: `instrument_type` が ETF または 投資信託、あるいは保有がすべて NISAつみたて投資枠
  - `loss_review`（減らす）: UC-011 の対象抽出条件（含み益率 ≤ 整理検討ライン、叩き台 -20%）
  - `take_profit`（減らす）: UC-004 の対象抽出条件（含み益率 > 動的利確ライン、かつ シグナルあり or 高水準モード該当）
  - `add_on`（増やす）: UC-010 の対象抽出条件（押し目買いシグナルあり ＋ 財務 `passed`/`unavailable` ＋ 全シグナル共通の前提条件）
  - `hold`（保つ）: 上記いずれにも該当しない個別株（JP/US）
  - `new_entry`（増やす）: UC-012 の未保有ウォッチリスト銘柄。**評価額構成比には算入しない**（保有していないため）。参考リストとして併置する
- **優先順位（排他解決、ADR-0014 D3、確定）**: 1銘柄はちょうど1つのバケツに属する。`core_accumulation` を `instrument_type`／口座区分で最初に確定し、以降は `loss_review` > `take_profit` > `add_on` > `hold` の順で最初に一致したバケツを採る。`take_profit` と `add_on` は ADR-0007 により元データで既に排他。`take_profit` と `loss_review` は含み益・含み損が同時成立しないため競合しない
- **落ちなかったバケツの注記（ADR-0014 D3、確定）**: `loss_review` に分類されたが押し目買いシグナルも発生している銘柄は、行内に「押し目買いシグナル発生中・買い増し候補にも掲載」と注記する（UC-011 の既存挙動と同一）。情報自体は抑制しない
- **リバランスの扱い（ADR-0014 D4、確定：バッジ止まり）**: セクター偏りは個別銘柄のバケツを動かさない。偏り警告セクターに属する銘柄の行に「偏り警告セクター」バッジを付し、画面上部に `sector_overweight_summary` を表示する。セクター単位の売却額目安は従来どおり UC-005 で提供する。`rebalance` サブバケツは作らない
- **評価額構成比（ADR-0014 D6）**: 各バケツの `Σ market_value ÷ 全保有 Σ market_value`。`market_value` は UC-004/UC-010 と同じ算出（保有数量 × 現在値、US株はCSVヘッダーの参考為替レートで円換算済み、投資信託は評価額）。基本は全口座ビューとし、課税口座のみビューは第2段階。`new_entry` は分母・分子に含めない
- **キープのヘルスラインと要観察（ADR-0014 D5、確定：第1段階から表示）**: `hold` バケツの各行に、含み益率・RSI帯・財務健全性の合否・相対力〔対市場〕の符号を簡易表示する。加えて「要観察（`hold_watch`）」フラグ（財務 failed・相対力継続マイナス等での早期警戒。既存の評価値の組み合わせのみで判定し、新しい閾値は作らない）を**第1段階から**表示する。「好調で優先的に強調したいキープ銘柄」の判定は、判定基準（相対力・RSI健全域等の組み合わせ）が現時点でどのUCにも存在せず新規閾値の発明になるため本CRでは見送り、`docs/product/accuracy-improvement-backlog.md` に将来検討事項として記録する
- **バケツ内ソート順（ADR-0014 D10、2026-09-17新規確定）**: 新しい閾値・重み付けは作らず、既存の算出値のみを用いる
  - `add_on`：既存流用（`ShowBuySignalListAction`）。①財務健全性（`passed`優先）②買いシグナル数（多い順）③含み益率（低い順）
  - `loss_review`：既存流用（`ShowLossReviewListAction`）。①押し目買いシグナル有無（無→優先）②財務ランク（failed優先）③テクニカル達成数（多い順）④含み損率（深い順）
  - `new_entry`：既存流用（`ShowWatchlistAction`）。①押し目買いシグナル数（多い順）②財務健全性③同セクター保有比率④52週レンジ内位置
  - `take_profit`：**新規設計**。①シグナル数（多い順）②判定チェックリストのテクニカル達成数（多い順）③含み益率（高い順）。UC-004本体（`ShowSignalListAction`）に実装し、UC-004画面自体もこの並び順になる（2026-09-17新規追加、下記「対象範囲」参照）
  - `hold`：**新規設計**。① `hold_watch` フラグが立っている銘柄を先頭に ② 残りは含み損益率が低い順
  - `core_accumulation`：**新規設計**。評価額（`market_value`）が大きい順
  - セクター偏りサマリ：**新規設計**。配分率の超過幅（`allocation_rate` − 70%）が大きい順
- **対象範囲**: 減らす／増やす側の分類は JP株・US株の個別株のみ。ETF・投資信託は常に `core_accumulation`（保つ）に入る。CFD は対象外（UC-002 業務ルール準拠）。**本CRはUC-004（`ShowSignalListAction`）に並び順ロジックを1件追加する（ADR-0014 D10-1）ため、「既にGreenのUC-001〜UC-012は無改修」という当初方針の唯一の例外となる**（判定条件・`signals`永続化には影響しない）
- **表示のみ・永続化なし（ADR-0014 D8/D9-2）**: 本UCは直近スナップショットを毎回その場で分類する表示専用機能であり、`signals`／`buy_signals`／`FetchExternalMarketDataAction`・DBスキーマには一切影響しない（CHG-0006／CHG-0010 と同方式）。分類結果の週次永続化と「先週→今週の異動」表示は第2段階の別CRで扱う
- **画面配置・UC-009との関係（ADR-0014 D9、確定：置き換え）**: グローバルナビの「サマリーレポート」タブ（UC-009）の最上部に「分類俯瞰」セクションとして追加する。新規タブは作らない（`ui-guidelines.md` のタブ数上限6個を維持）。UC-009 の旧・上位10〜20件レコメンド（非開示合成スコア、ADR-0003）は本UCに完全に置き換わる（併存しない）。UC-009側で独自実装していた候補選定ロジック（`buildTakeProfitCandidates`/`buildRebalanceCandidates`/`buildNewCandidateItems`/`composite_score`）は退役する。詳細はADR-0014 D9-1〜D9-4、UC-009参照
- あくまで俯瞰・気づきのための参考情報であり、自動発注は行わない（`requirements.md` OUTスコープに従う）

#### エラーケース
| ケース | HTTPステータス | メッセージ |
|---|---|---|
| スナップショットを持つ取込バッチが1件も存在しない | - | 空状態として「まだCSVの取込がありません」を表示し、CSV取込（UC-001）への導線を示す |
| 直近スナップショットに保有銘柄が存在しない | - | 空状態として「分類対象の保有銘柄がありません」を表示 |
| 一部銘柄の指標が未取得で `take_profit`／`add_on` 判定ができない | - | 当該銘柄は判定可能な条件のみで分類し（多くは `hold` に落ちる）、行内に「指標取得中／取得不可」の注記を出す。画面全体の表示は継続する |

#### 権限
- 本人: 実行可（他Actorなし）

---

## 承認記録

| 日付 | レビュアー | 結果 | コメント |
|---|---|---|---|
| 2026-08-15 | minowaryo | 承認 | UC-001〜UC-009を一括承認。UC-009（取込後サマリーレポート・F-009）およびUC-001のCSV3ファイル入力への変更を含む。なお`docs/product/mockups/README.md`上ではUC-001追加変更分・UC-003・UC-004・UC-009のビジネスレビューが「未実施」のまま残っているが、本承認をもってuse-cases.mdの内容として確定する（詳細は`PLAN.md`参照） |
| 2026-08-21 | minowaryo | 承認（CR） | UC-009業務ルールを改訂（CHG-0002、ADR-0003）。合成スコアの計算式・重み付けは引き続き非開示のままとしつつ、`portfolio_headline`・`reason_summary`に判定の主要因となった代表指標を含めることを必須化。UC-009はPhase 1未着手のため既存実装への影響なし |
| 2026-08-21 | minowaryo | 承認（CR） | 分析エンジンの指標セットを拡張（CHG-0003、ADR-0004）。テクニカル指標に出来高・52週高値/安値・相対力（対市場・対セクター）、ファンダメンタルズ指標にEPS成長率・PEGレシオを追加。UC-001に外部データ取得フロー（フロー7〜8）を追加、UC-004のシグナル種別を4種追加（`week52_high_pullback`/`peg_overvalued`/`relative_strength_weakening`/`volume_spike_decline`）、UC-007の市場指標取得・保存ロジックをPhase1に先行実装。**UC-003・UC-009は実装済み（Green完了、UC-009は本CR起票と並行して別セッションで完了・後日判明）のため、本CRの反映には両UCへの追加のTDDサイクル（Red→Green、Gate4再承認）が必要** |
| 2026-08-23 | minowaryo | 承認 | UC-010（既存保有株の買い増しタイミングレコメンド、F-010、ADR-0007）を新規承認。承認時のレビューで「健全で好調だった銘柄が市場全体・セクター全体の調整で一時的に下げた場面を拾う」という意図の担保が不十分と判明し、全シグナル共通の前提条件（直近13週以内の52週高値-15%以内到達歴、相対力〔対市場〕-5pt以上）を追加した上で承認（業務ルール参照）。数値パラメータは他UC同様、叩き台のままGate4実装時に確定する方針 |
| 2026-08-27 | minowaryo | 承認（CR） | UC-010実装完了後の`/review`で、ファンダメンタルズ健全性フィルタにUC-010のみ成長率条件を追加した結果、UC-008/UC-009（同一基準を謳っていたにもかかわらず自己資本比率・ROEの2条件のみ）と判定結果が乖離し得ることが判明（CHG-0005）。UC-008の業務ルールを改訂し、UC-008/UC-009もROE・自己資本比率・成長率の3条件に統一する。UC-008・UC-009は既にGreen完了のため、両UCへの追加のTDDサイクル（Red→Green、Gate4再承認）が必要 |
| 2026-08-29 | minowaryo | 承認（CR） | 売買シグナル画面（UC-004利確検討・UC-010買い増し候補）の一覧に「判定チェックリスト」（`criteria`）を追加（CHG-0007）。判定に使う項目の基準点・実測値・達成状態（達成／あと一歩／未達／データなし）を並べて表示し、良し悪しの判断をしやすくする。基準値は既存の確定済みシグナル判定閾値・`FundamentalHealthEvaluator` の閾値をそのまま可視化するもので、新しい判定基準値は設けない（`near`〔あと一歩〕バッファのみ `data-model.md` で新規確定）。表示のみの機能で `signals`/`buy_signals` の判定・永続化には影響しない。「一覧に全指標の内訳を出さない」という `ui-guidelines.md` テーブル方針の例外として承認。UC-004・UC-010は既にGreen完了のため、両UCへの追加のTDDサイクル（Red→Green、Gate4再承認）が必要 |
| 2026-09-05 | minowaryo | 承認（CR） | 取込後サマリーレポート（UC-009）をグローバルナビゲーションの「サマリーレポート」タブからいつでも再表示できるようにする（CHG-0008）。取込直後のリダイレクト表示（既存フロー1〜6）に加え、フロー7として恒常的な再表示手段を追加。対象はスナップショットを持つ最新の取込バッチ1件のみで、開くたびに最新指標で再計算する（過去バッチの履歴閲覧は対象外）。合成スコアリングロジック・`import_summary_reports`/`import_summary_report_items`のデータモデル・既存の取込直後フロー・JSON APIはいずれも変更なし。取込バッチが1件も無い場合の空状態表示をエラーケースに追加。`ui-guidelines.md`のナビゲーション構成を5タブから6タブに更新（同ファイルの上限目安「5〜6個程度」の範囲内） |
| 2026-08-28 | minowaryo | 承認（CR） | 「【検討事項・未着手】利確・リバランス閾値の動的分岐ロジック検討」（2026-08-22記録）をPlanフェーズで具体化し承認（CHG-0006）。UC-004・UC-009の利確検討対象抽出・分割指値提案に「通常モード（含み益+20%超、既存の閾値）」「高水準モード（シグナル0件かつ財務健全性フィルタ〔`FundamentalHealthEvaluator`〕を満たす銘柄のみ、含み益+150%超、分割指値+100%/+150%）」の2モードを導入。判定ロジックは表示・集計レイヤー（`ShowSignalListAction`・`ShowImportSummaryReportAction`）のみで完結させ、`FetchExternalMarketDataAction`のシグナル判定・永続化条件は変更しないためUC-010への影響はない設計とした。UC-004・UC-009は既にGreen完了のため、両UCへの追加のTDDサイクル（Red→Green、Gate4再承認）が必要 |
| 2026-09-05 | minowaryo | 承認（CR） | 整理検討（含み損）候補一覧（UC-011、F-011、ADR-0010）を新規承認。`requirements.md` 2章OUT「含み損銘柄の売却判断支援」を「一覧表示による判断材料の提示」に限りINへ移す部分改訂（F-010がADR-0007で押し目買いに限りOUTを覆したのと同じ構図）。`/signals`画面の第3セクションとして、含み損-20%超の個別株に復帰必要上昇率・含み損実額・連続保有週数・財務健全性の合否・UC-004面対称の判定チェックリストを表示する。機械的な損切り指示・分割売却の指値提案・自動発注は行わない。ファンダ`failed`銘柄・押し目シグナル発生銘柄も除外せず順序制御で対応。並び順は透明なマルチキー（F-009合成スコアは不使用）。新規閾値3件（整理検討ライン-20%／52週高値-30%／押し目0件）はGate3で確定。DBスキーマ変更なし・表示レイヤー完結（CHG-0006と同方式）のため`signals`/`buy_signals`/`FetchExternalMarketDataAction`は不変。Gate1（requirements.md 2章OUT改訂・F-011追加・7章）承認済み |
| 2026-09-05 | minowaryo | 承認（CR） | 売買シグナル画面（UC-004利確検討・UC-010買い増し候補）の一覧の各行に「評価額」（`market_value` ＝ 保有数量 × 現在値。US株はCSVヘッダーの参考為替レートで円換算済みの `current_price` を用いるため円建て）を、銘柄名の右隣（2列目）に追加（CHG-0011）。CSV由来の数量・現在値から算出する表示専用項目であり、`signals`/`buy_signals` の判定・永続化ロジック、`criteria`（判定チェックリスト）、対象抽出条件にはいっさい影響しない。DBスキーマ変更なし（Gate3対象外）。UC-004・UC-010は既にGreen完了のため、両UCへの追加のTDDサイクル（Red→Green、Gate4再承認）が必要 |
| 2026-09-06 | minowaryo | 承認（CR） | 整理検討（含み損）候補一覧（UC-011）の判定チェックリスト配色を「赤の単一極性」に変更（ADR-0010 D6 改訂、CHG-0010）。設計レビューで、1テーブル内にテクニカルの緑（＝売り後押し）と財務の緑（＝保留材料）が同居すると同じ色が正反対を意味し、サマリバッジが「合計◯/10＝売り確定」と誤読される懸念が判明。対策として財務健全性3項目の判定の向きを反転し、「基準割れ（＝投資根拠の毀損）」を `met`（赤チップ）とする。閾値の値（ROE 10%／自己資本比率 40%／成長率 0%）は既存流用のまま。整理検討テーブルのチップ配色は達成＝赤系／未達・データなし＝グレーとし、緑は使わない（利確検討・買い増し候補テーブルは不変）。UC-011 業務ルール「判定チェックリスト」節の財務健全性3項目・達成状態の記述を改訂。反転は表示レイヤーのみ（`SignalCriteriaEvaluator::fundamentalRows()` に反転フラグ追加）で `signals`/`buy_signals` の判定・永続化には影響しない。DBスキーマ変更なし |
| 2026-09-05 | minowaryo | 承認（CR） | 米国株のファンダメンタルズ指標データソースとしてFinnhub APIを新規採用（CHG-0009、ADR-0009）。ADR-0004/ADR-0007が「US株はファンダメンタルズ指標のデータソースが未実装」としていた既知の制約を解消し、PER・PBR・ROE・売上高/営業利益成長率・自己資本比率・配当利回り/性向・EPS成長率・PEGレシオの全8指標をJP株と同様に実数値化する。UC-001フロー7（データソースの記載追加）、UC-003（J-Quants12週遅延の注記をJP株限定に限定し、US株はFinnhub由来で当該制約が適用されない旨を追記）、UC-004（判定チェックリストの`unavailable`説明を修正）、UC-010（US株の健全性フィルタ判定に関する記載を修正）を改訂。自己資本比率は`totalDebt/totalEquity`からの近似ではなく`financials-reported`（10-KのXBRL実データ）から実測計算する方式を採用（近似では実測比最大約2倍の誤差を検証で確認したため不採用）。`fundamental_indicators`テーブルは市場非依存の既存スキーマのままDBマイグレーション不要（Gate3は影響範囲の確認のみ）。UC-003/UC-004/UC-010は既にGreen完了のため、各UCへの追加のTDDサイクル（Red→Green、Gate4再承認）が必要 |
| 2026-09-08 | minowaryo | 承認（CR、Gate1/2/3一括） | **お気に入り未保有銘柄ウォッチリスト（UC-012、F-012、ADR-0013）を新規承認**。UC-006（重複チェック）／UC-008（軽量レコメンド）を統合・刷新し、`/candidate-check` を全面刷新する。楽天証券お気に入り銘柄CSV（216銘柄）を取り込み、未保有銘柄について保有銘柄と同じ外部データソースから指標を取得（保有限定だった `FetchExternalMarketDataAction` のスコープを未保有銘柄へ拡張する仕組みを新設）、押し目買いシグナル・財務健全性フィルタ・同セクター保有比率・52週レンジ内位置による透明マルチキーソートで候補一覧を提示する。買いシグナルは `holding_snapshot_id` を持てないため専用テーブル `watchlist_buy_signals` に保存（`buy_signals` は不変）。データ取得はキュー非同期実行＋進捗表示（`compose.yaml` に `queue:work` サービス追加）。★お気に入りの手動登録・解除、CSV再取込は追加のみ。UC-006 の重複度・ウォッチメモ・過去業績推移は一覧の行展開に引き継ぎ、「銘柄コード手入力の入口」「注目テーマの手動登録」は廃止。`NewCandidateFinder`／`watched_themes` はF-005が流用するためコードは残置。新テーブル3件（`watchlist_items`／`watchlist_buy_signals`／`watchlist_refresh_runs`）、既存テーブルの変更なし。実装着手はF-011マージ後。閾値・パラメータ（ソートキー・52週レンジ内位置の算出）はGate3で確定 |
| 2026-09-06 | （Gate2/Gate3レビュー待ち） | 提案（CR） | 財務健全性フィルタに**営業利益率（`operating_margin`）を4条件目**として追加（CHG-0012、ADR-0011。Phase 0 ドキュメント先行）。UC-001フロー7・UC-003フロー4（ファンダメンタルズ指標一覧に営業利益率を追加）、UC-004/UC-010/UC-011（判定チェックリストの財務健全性を3→4項目、`criteria.fundamental` 4項目、閾値10%）、UC-005/UC-008/UC-009（健全性フィルタの条件に営業利益率を追加）を改訂。閾値10%は叩き台（8%案との実測比較の経緯はADR-0011。実測影響: 財務 `passed` が合計 27→22）。JP株は既取得の `net_sales`/`operating_profit` から実測算出、US株はFinnhub `operatingMarginTTM` 採用（新規APIコールなし）。**`fundamental_indicators` に `operating_margin` 列を1つ追加するマイグレーションが必要**（CHG-0010・CHG-0011 と異なりスキーマ変更あり）。UC-003/004/005/008/009/010/011 は既にGreen完了のため、各UCへの追加のTDDサイクル（Red→Green、Gate4再承認）が必要。実装着手は F-011（`feat/f011-loss-review-list`）マージ後 |
| 2026-09-08 | minowaryo | 提案（CR） | **ポートフォリオ分類ダッシュボード（UC-013、F-013、ADR-0014）を新規提案**。UC-004（利確検討）/ UC-005（セクター配分）/ UC-010（買い増し候補）/ UC-011（整理検討）/ UC-012（ウォッチリスト）の抽出結果を銘柄起点で1つのバケツ（`core_accumulation`／`loss_review`／`take_profit`／`add_on`／`hold`／`new_entry`）に束ね直し、「減らす／保つ／増やす」の3大分類ごとに評価額構成比・銘柄リストを俯瞰表示する。**新しい抽出条件・閾値は設けず、既存UCの対象抽出ロジックを再投影する**（ADR-0014 D2）。1銘柄はちょうど1つのバケツに属し、優先順位（`core_accumulation` を先に確定 → `loss_review` > `take_profit` > `add_on` > `hold`）で排他解決する（D3）。リバランス（セクター偏り）は個別銘柄のバケツを動かさず、偏り警告バッジ＋セクターサマリで表示（D4）。「保つ」は積立コア／キープの内訳を明示（D6）。キープには簡易ヘルスラインを表示（D5）。画面はサマリーレポートタブ（UC-009）の最上部に「分類俯瞰」セクションとして追加し、新タブは作らない（D9、`ui-guidelines.md` タブ数6個維持）。**第1段階は表示レイヤー完結（`ClassifyHoldingsAction` 相当の純ロジック＋Blade追加のみ、DBスキーマ変更なし・永続化なし、CHG-0006／CHG-0010 と同方式）。「先週→今週の遷移表示」と分類の週次永続化（`portfolio_classifications` テーブル）は第2段階の別CR**。実装着手は F-012 マージ後を予定し、Gate2確定（5つの判断ポイント）は2026-09-17に持ち越し（詳細は下記行） |
| 2026-09-17 | minowaryo | 承認（CR、Gate1/2最終確定） | **ポートフォリオ分類ダッシュボード（UC-013、F-013、ADR-0014）Gate1（requirements.md）／Gate2（use-cases.md）を最終承認**。2026-09-08提案時点の5つの判断ポイントをすべて確定: (1) 排他優先順位は叩き台通り採用、(2) `loss_review`×`add_on`競合は`loss_review`採用＋注記、(3) リバランスはバッジ＋セクターサマリ止まり（`rebalance`サブバケツは作らない）、(4) `hold_watch`（要観察）は第1段階から表示、(5) UC-009の旧・上位10〜20件レコメンド（非開示合成スコア、ADR-0003）は分類俯瞰に**完全に置き換え**（併存しない）。(5)の実装確認の過程で、UC-009側が既存UCを再利用せず独自の候補選定ロジック（`buildTakeProfitCandidates`/`buildRebalanceCandidates`/`buildNewCandidateItems`/`composite_score`/`import_summary_reports`永続化）を重複実装していたことが判明し、置き換えにあわせて退役させることも確定（ADR-0014 D9-1〜D9-4）。あわせてGate2レビュー中に新規発生した論点も確定: 永続化廃止（`import_summary_reports`/`import_summary_report_items`への書き込み停止、`ImportCsvAction`のプレースホルダー行作成も削除）、`portfolio_headline`をバケツ件数の集計文に変更、各行の一言評価（`bucket_reason`）は`SignalCriteriaEvaluator`の達成度データから機械生成する簡潔な理由文に限定、バケツ内ソート順を全バケツ分新規確定（`add_on`/`loss_review`/`new_entry`は既存流用、`take_profit`は新規設計しUC-004本体`ShowSignalListAction`に実装、`hold`は`hold_watch`優先→含み損益率順、`core_accumulation`は評価額順、セクター偏りサマリは超過幅順）、ADR-0003をSupersededと明記、`WatchedTheme`ベースの新規候補ロジックは退役（実データ確認済み・登録0件のため実質影響なし、モデル自体はF-005用に残置）。`hold`内の「好調キープ強調」・20%アクティブ枠の予算トラッキング（D7）は本CRのスコープ外とし`accuracy-improvement-backlog.md`に記録。UC-004（`ShowSignalListAction`への並び順追加）を除き、UC-001〜UC-012の既存ロジックは無改修。第1段階はDBスキーマ変更なし（Gate3は影響範囲確認のみ）。実装着手はF-012マージ後（マージ済み、`feat/f013-portfolio-buckets`等の新ブランチで着手可能） |
| 2026-09-19 | minowaryo | 承認（CR、Gate1/2） | **バリュー/景気敏感銘柄向け判定ロジック分岐（CHG-0017、ADR-0015）をGate1（requirements.md）／Gate2（use-cases.md）承認**。`docs/product/accuracy-improvement-backlog.md`「売買戦略の深化ロードマップ」節で特定した「次の1手」。`FundamentalHealthEvaluator`の成長率判定が単年度のみでバリュー/景気敏感株を取りこぼす問題・PEG判定（売り側`peg_overvalued`／買い側`peg_undervalued`）が低成長銘柄で機能不全になる問題・買い増し事前条件が対市場相対力のみでセクター相対を無視する問題・押し目買いに中期トレンド確認が無い問題の4点を是正する。銘柄のセクター分類から機械的に導出する「スタイル（growth/value_cyclical）」を新設（新規DBカラムなし）。UC-004/UC-010に(1)財務健全性の成長率項目へ「直近3〜5期平均成長率>0%」のOR救済分岐（全銘柄共通）、(2)value_cyclical銘柄はPEG判定を除外しPER≦15かつ配当利回り≧3%〔暫定閾値〕の絶対閾値に代替、(3)買い増し事前条件を`relative_strength_vs_sector`優先・`relative_strength_vs_market`フォールバックに変更、(4)押し目買いに週足MA75の中期トレンド確認（13週前比で上向き）を追加。UC-004には`valuation_zone_badge`（絶対バリュエーション上は割安ゾーンの補助バッジ）を追加するが、UC-010への同時掲載は行わない。UC-011は`FundamentalHealthEvaluator`共有のため(1)の影響を自動的に受ける。判定の主軸（含み益率ベース）は維持し、資料3⑤（PEG合否基準の明文化・成長率上限レンジ）は本CRのスコープに含めない（いずれも本人確認済み、2026-09-19）。(2)と(1)は同一設計単位（(1)単独では`peg_overvalued`シグナルが`signalCount`を非ゼロに保ち続け、`TakeProfitThresholdEvaluator`の高水準モードに到達できないため）。技術トレンド条件の実装に伴い`technical_indicators`へ軽量な新規カラムを追加する小規模migrationが必要（Gate3で確定）。UC-004/UC-010/UC-011は既にGreen完了のため、各UCへの追加のTDDサイクル（Red→Green、Gate4再承認）が必要。実装着手はF-013と作業ツリーが衝突しないよう別ブランチで進める |
| 2026-09-19 | minowaryo | 承認（CR、Gate4 Cycle1着手時改訂） | **CHG-0017（ADR-0015）のD1・D3を改訂**。実データ確認で`sector_classification_id`がJP保有146銘柄中7銘柄しか埋まっておらず（CRの発端である伊藤忠8001も未分類）、セクター分類ベースの「スタイル（growth/value_cyclical）」判定は実効性がないと判明。加えて本人から「セクター分類自体に重きを置いていない」「食品はgrowthではない、建設も文脈次第」との指摘があり、業種名による二分自体を撤回。財務指標のみで判定する方式に変更: 財務健全性の成長率項目に③ROE≧15%かつ配当利回り≧3%かつ自己資本比率≧50%の救済経路を追加（全銘柄対象、セクター非依存）、PEG除外の発動条件を「value_cyclical銘柄」から「成長率が低い（5%以下、暫定）銘柄」に変更。UC-004/UC-010の該当箇所（判定チェックリスト・`valuation_zone_badge`）を改訂。D2（3〜5期平均成長率）・D4（対セクター相対力フォールバック）・D5（中期トレンド確認）は変更なし |
| 2026-09-19 | minowaryo | 承認（CR、Gate4レビュー時再改訂） | **CHG-0017（ADR-0015）D1の閾値をさらに改訂**。「配当利回り≧3%」をAND条件から削除し、D1＝「ROE≧15%かつ自己資本比率≧50%」のみに簡素化。無配当・低配当でも自社株買い等で資本効率を高める銘柄（特に米国株）を機械的に除外してしまうとの本人指摘を受け、実測比較（保有218銘柄中、成長率のみで`failed`/`unavailable`になる候補2件のうち、配当必須なら救済0件・配当不問なら2件救済）のうえ決定。配当利回りは判定に使わず理由サマリの参考情報として表示する。D3（PEG除外時のPER≤15かつ配当利回り≥3%）は別の判定目的のため変更なし |
